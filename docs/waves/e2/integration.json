{
  "wave": "E2",
  "status": "passed",
  "checks": 95,
  "assertions": [
    "native highloadblock installation",
    "native module Loader after merged B1 and E1",
    "native E2 controller DI resolves",
    "COM-01 actual empty catalogue and common pagination defaults",
    "successful private responses are not cacheable",
    "missing bearer",
    "missing bearer: canonical private error envelope",
    "invalid bearer",
    "invalid bearer: canonical private error envelope",
    "role denied: no profile",
    "role denied: no profile: canonical private error envelope",
    "mutation denied: no profile",
    "mutation denied: no profile: canonical private error envelope",
    "role denied: teacher",
    "role denied: teacher: canonical private error envelope",
    "mutation denied: teacher",
    "mutation denied: teacher: canonical private error envelope",
    "role denied: head",
    "role denied: head: canonical private error envelope",
    "mutation denied: head",
    "mutation denied: head: canonical private error envelope",
    "role denied: curator",
    "role denied: curator: canonical private error envelope",
    "mutation denied: curator",
    "mutation denied: curator: canonical private error envelope",
    "no delete route exists",
    "malformed JSON",
    "malformed JSON: canonical private error envelope",
    "invalid object shape null",
    "invalid object shape null: canonical private error envelope",
    "invalid object shape []",
    "invalid object shape []: canonical private error envelope",
    "invalid object shape {}",
    "invalid object shape {}: canonical private error envelope",
    "strict JSON field price",
    "strict JSON field price: canonical private error envelope",
    "strict JSON field active",
    "strict JSON field active: canonical private error envelope",
    "strict JSON field description",
    "strict JSON field description: canonical private error envelope",
    "strict JSON field actorId",
    "strict JSON field actorId: canonical private error envelope",
    "required idempotency key",
    "required idempotency key: canonical private error envelope",
    "invalid idempotency key",
    "invalid idempotency key: canonical private error envelope",
    "query cannot override JSON",
    "query cannot override JSON: canonical private error envelope",
    "JSON content type required",
    "JSON content type required: canonical private error envelope",
    "unknown query rejected",
    "unknown query rejected: canonical private error envelope",
    "page size bound",
    "page size bound: canonical private error envelope",
    "invalid and forbidden requests left catalogue empty",
    "COM-02 creates UUID with revision and Location",
    "normalized same-key create replays exact result status and Location",
    "key mismatch rejected",
    "key mismatch rejected: canonical private error envelope",
    "idempotency key is scoped by actor",
    "COM-03 PATCH scopes same key by method/resource",
    "PATCH replay precedes now-stale global revision check",
    "real mapper uses name ordering and preserves false zero empty",
    "exact Product projection without persistence or auth fields",
    "native pagination is stable name asc then internal id",
    "catalogue revision is global across products",
    "catalogue revision is global across products: canonical private error envelope",
    "missing product",
    "missing product: canonical private error envelope",
    "strict path UUID",
    "strict path UUID: canonical private error envelope",
    "PATCH null does not clear nonnullable field",
    "PATCH null does not clear nonnullable field: canonical private error envelope",
    "stored replay does not bypass disabled staff",
    "stored replay does not bypass disabled staff: canonical private error envelope",
    "stored replay does not bypass revoked token",
    "stored replay does not bypass revoked token: canonical private error envelope",
    "install isolated failure trigger",
    "idempotency persistence failure rolls back mutation",
    "idempotency persistence failure rolls back mutation: canonical private error envelope",
    "failed response persistence leaves product and revision unchanged",
    "failed key can retry successfully",
    "missing access state fails closed",
    "missing access state fails closed: canonical private error envelope",
    "real concurrent workers wait on actor authorization lock",
    "concurrent identical HTTP requests return one durable result",
    "concurrent retry creates one product and one revision",
    "replay survives a fresh PHP process",
    "token revoked after prefilter is rejected under transaction locks",
    "token revoked after prefilter is rejected under transaction locks: canonical private error envelope",
    "role changed after prefilter is rejected under transaction locks",
    "role changed after prefilter is rejected under transaction locks: canonical private error envelope",
    "authorization races leave catalogue and revision unchanged",
    "denied races create no idempotency receipts",
    "rollback preserves durable mutation receipts"
  ],
  "php": "8.4.23",
  "bitrix": "25.750.0",
  "mysql": "8.0.45",
  "seams": [
    "CLI reconstructs native HttpRequest; only php://input is supplied by a test subclass.",
    "Each request uses actual main route registry, Router, controller, filters, request factory, serializer and database.",
    "Concurrent independent PHP workers reconnect to the same disposable fixture; pause adapter wraps real Access locks to coordinate races."
  ],
  "externalNetwork": false,
  "hostPorts": false,
  "productionData": false
}
