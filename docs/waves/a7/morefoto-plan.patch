diff --git a/docs/04-bitrix-modules/backend-waves.json b/docs/04-bitrix-modules/backend-waves.json
--- a/docs/04-bitrix-modules/backend-waves.json
+++ b/docs/04-bitrix-modules/backend-waves.json
@@ -2,2 +2,2 @@
-  "version": 1,
-  "planStatus": "proposed",
+  "version": 2,
+  "planStatus": "active",
@@ -6,0 +7,45 @@
+  "baseline": {
+    "branch": "main",
+    "commit": "2ac4ef9b3e4b63d734fc3dda5240cab50a550d44",
+    "mergedWaves": [
+      "A1",
+      "A2",
+      "A3",
+      "A4",
+      "A5",
+      "A6"
+    ],
+    "review": {
+      "B1": "https://github.com/rebit-pro/rabit-api/pull/8"
+    },
+    "note": "Снимок состояния, не автоматическая проверка GitHub; перед стартом и merge сверять актуальный main."
+  },
+  "acceptedImplementationDecisions": [
+    "D07",
+    "D08",
+    "D10"
+  ],
+  "directions": {
+    "A": "Основа и правила",
+    "B": "Access",
+    "C": "Organization",
+    "D": "Media",
+    "E": "Commerce",
+    "F": "Handoff",
+    "G": "Payments",
+    "H": "Notification",
+    "I": "Settlement",
+    "J": "Files",
+    "K": "Support",
+    "L": "Production",
+    "M": "Shipping",
+    "N": "Итоговые проекции и приёмка"
+  },
+  "mergePolicy": {
+    "baseBranch": "main",
+    "dependenciesMustBeMerged": true,
+    "stackedPullRequests": false,
+    "verifyMainPlusOwnDiff": true,
+    "decisionGatesMustBeResolved": true,
+    "readinessNote": "unlocks — прямые потребители. Merge одной волны разблокирует потребителя только после merge остальных dependsOn и решения его открытых decisionGates. inProgress/review не равны merged; A7 не runtime gate."
+  },
@@ -9 +54 @@
-      "id": "W00",
+      "id": "A1",
@@ -42,4 +87,14 @@
-      "activation": "Сохранить действующие Lead/LeadHunter. Никаких реальных сообщений или миграций ради инвентаризации."
-    },
-    {
-      "id": "W01",
+      "activation": "Сохранить действующие Lead/LeadHunter. Никаких реальных сообщений или миграций ради инвентаризации.",
+      "legacyIds": [
+        "W00"
+      ],
+      "deliveryState": "merged",
+      "unlocks": [
+        "A2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: исходная основа. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции AUTH-03, AUTH-04, NTF-01, INF-01; Есть воспроизводимая точка отсчёта; исходники отделены от проверки работающего HTTP.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "A2",
@@ -52 +107 @@
-        "W00"
+        "A1"
@@ -74,4 +129,15 @@
-      "activation": "Исправление основы независимо от R17; продуктовые маршруты не включать."
-    },
-    {
-      "id": "W02",
+      "activation": "Исправление основы независимо от R17; продуктовые маршруты не включать.",
+      "legacyIds": [
+        "W01"
+      ],
+      "deliveryState": "merged",
+      "unlocks": [
+        "A3",
+        "H1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: A1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Без новых публичных HTTP-операций; Запрос, ответ и ошибка логируются без паролей, кодов, Bearer и capability-ключей.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "A3",
@@ -84 +150 @@
-        "W01"
+        "A2"
@@ -110,4 +176,16 @@
-      "activation": "Основа Auth исправляется до R17. Роли и назначения MoreFoto начинаются с W06."
-    },
-    {
-      "id": "W03",
+      "activation": "Основа Auth исправляется до R17. Роли и назначения MoreFoto начинаются с B1.",
+      "legacyIds": [
+        "W02"
+      ],
+      "deliveryState": "merged",
+      "unlocks": [
+        "A4",
+        "B1",
+        "B2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: A2. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции AUTH-01, AUTH-02; Истёкший/повреждённый токен не даёт доступ; регистрация не оживляет заблокированного сотрудника.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "A4",
@@ -121 +199 @@
-        "W02"
+        "A3"
@@ -145,4 +223,15 @@
-      "activation": "Открывать SHR-01 после mapper/ownership gate; оригиналы и покупательская выдача — W10/W25."
-    },
-    {
-      "id": "W04",
+      "activation": "Открывать SHR-01 после mapper/ownership gate; оригиналы и покупательская выдача — D1/J1.",
+      "legacyIds": [
+        "W03"
+      ],
+      "deliveryState": "merged",
+      "unlocks": [
+        "A5",
+        "D1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: A3. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции SHR-01; Технический upload имеет проверенный вход и устойчивого владельца; приватные оригиналы проектируются отдельно.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "A5",
@@ -158 +247 @@
-        "W03"
+        "A4"
@@ -182,4 +271,16 @@
-      "activation": "Подготовить изолированный backend. Публикация и миграции production — отдельный шаг."
-    },
-    {
-      "id": "W05",
+      "activation": "Подготовить изолированный backend. Публикация и миграции production — отдельный шаг.",
+      "legacyIds": [
+        "W04"
+      ],
+      "deliveryState": "merged",
+      "unlocks": [
+        "A6",
+        "B1",
+        "H1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: A4. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Без новых публичных HTTP-операций; Новая волна подключается явной конфигурацией и проверяемыми миграциями, с планом отката кода.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "A6",
@@ -193 +294 @@
-        "W04"
+        "A5"
@@ -204 +305 @@
-        "Зафиксировать принятые модель, владельцев записей, матрицу и рабочие решения W05; одна компания-продавец, фотография может относиться к нескольким детям.",
+        "Зафиксировать принятые модель, владельцев записей, матрицу и рабочие решения A6; одна компания-продавец, фотография может относиться к нескольким детям.",
@@ -213 +314 @@
-        "Модель и входные решения W06–W09 приняты пользователем; канонические domain/database/API синхронизированы. Отдельная бизнес-проверка B01–B14 не объявлена выполненной.",
+        "Модель и входные решения B1, C1–C4 и B2 приняты пользователем; канонические domain/database/API синхронизированы. Отдельная бизнес-проверка B01–B14 не объявлена выполненной.",
@@ -216,4 +317,52 @@
-      "activation": "W06 разрешена после слияния проверенной основы W00–W05. Некритичные настройки уточняются по ходу; крупные изменения модели согласовываются заранее."
-    },
-    {
-      "id": "W06",
+      "activation": "B1 разрешена после слияния проверенной основы A1–A6. Некритичные настройки уточняются по ходу; крупные изменения модели согласовываются заранее.",
+      "legacyIds": [
+        "W05"
+      ],
+      "deliveryState": "merged",
+      "unlocks": [
+        "A7",
+        "B1",
+        "C1",
+        "E1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: A5. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Без новых публичных HTTP-операций; 11.09.2026 пользователь принял модель/схему Access/Organization и рекомендованные D07/D08/D10; разрешил продолжить реализацию. Бизнес-проверка R17 остаётся отдельной.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "A7",
+      "title": "Независимые волны и граф зависимостей",
+      "kind": "design",
+      "owners": [
+        "project.workflow"
+      ],
+      "dependsOn": [
+        "A6"
+      ],
+      "decisionGates": [],
+      "endpointIds": [],
+      "verifiesEndpointIds": [],
+      "outcome": "Каждый PR независимо проверяется и вливается в main; направление и номер заменяют последовательную очередь.",
+      "scope": [
+        "Обязательные правила main-only и отсутствие stacked PR; явные dependsOn/unlocks и обратная карта WNN.",
+        "Разделить учреждения и каталог на завершённые внутренние сценарии и зависимый защищённый HTTP; сохранить 99 API ID.",
+        "Генерация/валидация настоящего DAG и документация статусов без ложного объявления API реализованным."
+      ],
+      "review": [
+        "A7 не является runtime-зависимостью C1/E1/B1 и не блокирует их merge.",
+        "Все legacy ID сохраняются; payload/method/path API не меняются."
+      ],
+      "acceptance": [
+        "Нет циклов, неизвестных зависимостей и потерянных/дублированных владельцев API.",
+        "Проверка отрицательных fixtures для циклов и ложной готовности; регенерация и offline Postman."
+      ],
+      "activation": "Обязательное правило работы принято пользователем 11.09.2026; документ не включает runtime или внешние эффекты.",
+      "legacyIds": [],
+      "deliveryState": "inProgress",
+      "unlocks": [],
+      "inputBoundary": "Принятое пользователем правило независимых PR и исторический план. Код других волн не требуется.",
+      "outputBoundary": "Без новых публичных HTTP-операций; Каждый PR независимо проверяется и вливается в main; направление и номер заменяют последовательную очередь.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "B1",
@@ -227,3 +376,3 @@
-        "W02",
-        "W04",
-        "W05"
+        "A3",
+        "A5",
+        "A6"
@@ -250,7 +399,19 @@
-        "/me с допустимой пустой областью новой БД; реальные назначения перепроверяются W09."
-      ],
-      "activation": "Подключить тестовую служебную сессию. Полные назначения/панели — после W09."
-    },
-    {
-      "id": "W07",
-      "title": "Учреждения и реквизиты",
+        "/me с допустимой пустой областью новой БД; реальные назначения перепроверяются B2."
+      ],
+      "activation": "Подключить тестовую служебную сессию. Полные назначения/панели — после B2.",
+      "legacyIds": [
+        "W06"
+      ],
+      "deliveryState": "review",
+      "unlocks": [
+        "C2",
+        "B2",
+        "E2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: A3, A5, A6. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции ACC-01; Сотрудник получает /me, сервер проверяет действие и область независимо от меню.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "C1",
+      "title": "Учреждения: внутренние сценарии и хранение",
@@ -263 +424,81 @@
-        "W06"
+        "A6"
+      ],
+      "decisionGates": [],
+      "endpointIds": [],
+      "verifiesEndpointIds": [],
+      "outcome": "Учреждения сохраняются и читаются внутренними сценариями на текущем main; C1 не объявляет готовыми ORG endpoints.",
+      "scope": [
+        "Завершённые внутренние create/update/get/list учреждения: HL-хранение, стабильный UUID, revision/CAS, поиск и пагинация.",
+        "Repository, UseCase/DTO, DI, штатная установка и новая foundation-миграция с реальными проверками Bitrix/MySQL.",
+        "Вход — реквизиты учреждения; назначения принадлежат Access и не записываются в Organization. HTTP, съёмки, группы и summary в этот срез не входят."
+      ],
+      "review": [
+        "Никаких импортов morefoto.access, таблиц назначений, зависимостей от незамерженного B1 или публичных маршрутов.",
+        "Пустая БД — допустимое состояние списка; завершённый сценарий должен работать без соседних веток."
+      ],
+      "acceptance": [
+        "Установка/миграция и создание→чтение→изменение→поиск на чистой тестовой БД.",
+        "Неизвестный UUID, неверные реквизиты, stale revision, пагинация и детерминированная сортировка."
+      ],
+      "activation": "Только внутренние сценарии и тестовый CLI/DI; frontend редактор учреждения подключается в C2.",
+      "legacyIds": [
+        "W07"
+      ],
+      "deliveryState": "inProgress",
+      "unlocks": [
+        "C2"
+      ],
+      "inputBoundary": "Только основа A6 (включая A5) в main; реквизиты учреждения. Не нужны Access, назначения, съёмки и группы.",
+      "outputBoundary": "Без новых публичных HTTP-операций; Учреждения сохраняются и читаются внутренними сценариями на текущем main; C1 не объявляет готовыми ORG endpoints.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "E1",
+      "title": "Каталог: внутренние сценарии и хранение",
+      "kind": "implementation",
+      "owners": [
+        "morefoto.commerce"
+      ],
+      "dependsOn": [
+        "A6"
+      ],
+      "decisionGates": [],
+      "endpointIds": [],
+      "verifiesEndpointIds": [],
+      "outcome": "Каталог работает как внутренний сценарий на текущем main; коммерческие значения остаются редактируемыми и не считаются согласованными.",
+      "scope": [
+        "Завершённые внутренние create/update/list Product, таблица и общий catalogRevision; целые денежные единицы, валидация реквизитов и CAS.",
+        "Repository, UseCase/DTO, DI, штатная установка и новая foundation-миграция, пустой каталог без seed.",
+        "Хранение редактируемых реквизитов не утверждает ассортимент, цены, скидки и коммерческие правила D02. HTTP и расчёт покупки в этот срез не входят."
+      ],
+      "review": [
+        "Нет зависимости от Access, Media, сотрудников и незамерженных веток.",
+        "Цены из примеров не становятся production-конфигурацией; float для денег запрещён."
+      ],
+      "acceptance": [
+        "Создание→список→изменение, валидация и конфликт глобального catalogRevision на реальной БД.",
+        "Пустой каталог, независимая установка/DI и отсутствие новых публичных маршрутов."
+      ],
+      "activation": "Внутренние сценарии; защищённые COM-01–03 добавляются в E2, коммерческие условия в E3.",
+      "legacyIds": [
+        "W12"
+      ],
+      "deliveryState": "inProgress",
+      "unlocks": [
+        "E2"
+      ],
+      "inputBoundary": "Только основа A6 (включая A5) в main; редактируемые реквизиты Product. Не нужны Access, Media, сотрудники или утверждённые цены.",
+      "outputBoundary": "Без новых публичных HTTP-операций; Каталог работает как внутренний сценарий на текущем main; коммерческие значения остаются редактируемыми и не считаются согласованными.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "C2",
+      "title": "Учреждения: защищённые команды и назначения",
+      "kind": "implementation",
+      "owners": [
+        "morefoto.organization",
+        "morefoto.access"
+      ],
+      "dependsOn": [
+        "B1",
+        "C1"
@@ -271 +511,0 @@
-        "ORG-04",
@@ -275,18 +515,28 @@
-      "outcome": "Организатор ведёт учреждения через одного владельца данных.",
-      "scope": [
-        "Подключить morefoto.organization: список/создание/карточка/изменение учреждения, поиск и пагинация.",
-        "curatorId/headId изменять через минимальный контракт Access уже в этой волне; UI управления учётками — W09.",
-        "Миграции/индексы, Result→DTO и версия изменения. Обязательный ORG-04 summary должен быть определён в W05."
-      ],
-      "review": [
-        "Нет записи в чужие таблицы; сотрудник и область проверены до изменения.",
-        "Поле, для которого ещё нет поставщика, не заменяется фиктивными 0/null/пустой формой."
-      ],
-      "acceptance": [
-        "Создание/изменение/повтор/конфликт; руководитель не видит чужое учреждение.",
-        "Пагинация/фильтры на БД; ORG-03/05 перепроверить с управлением назначениями W09, полную ORG-04 — W33."
-      ],
-      "activation": "Переключить готовые операции редактора. Полную карточку с недоступными разделами не подключать до закрытия зависимостей."
-    },
-    {
-      "id": "W08",
+      "outcome": "Организатор ведёт учреждения через защищённые ORG-02/03/05; назначения изменяются через владельца Access.",
+      "scope": [
+        "Добавить HTTP к готовым сценариям C1, Bearer и серверную проверку роли/области B1.",
+        "Минимальный контракт institution assignments Access: curatorId/headId, проверка действующего сотрудника, assignmentSignature и атомарное изменение с ревизией учреждения. UI управления учётками остаётся B2.",
+        "Назначения не дублируются в Organization; ORG-04 вынесен в C4 до настоящих съёмок/групп C3."
+      ],
+      "review": [
+        "Неизвестная роль/чужое учреждение запрещены; установка обоих поставщиков уже в main.",
+        "Переназначение, версия и отзыв сессий выполняются атомарно через владельцев, без фиктивного поставщика."
+      ],
+      "acceptance": [
+        "Реальный HTTP list/create/update, валидация, чужой ID и conflict.",
+        "Назначения и список перепроверить после B2; отсутствующая карточка ORG-04 не объявляется реализованной."
+      ],
+      "activation": "Подключать только готовые операции списка/редактора, которые не требуют ORG-04; полная карточка — C4.",
+      "legacyIds": [
+        "W07"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "C3"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: B1, C1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции ORG-02, ORG-03, ORG-05; Организатор ведёт учреждения через защищённые ORG-02/03/05; назначения изменяются через владельца Access.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "C3",
@@ -299 +549 @@
-        "W07"
+        "C2"
@@ -316 +566 @@
-        "Съёмки, группы и свойства; teacherId и прочие назначения через Access.",
+        "Съёмки, группы и свойства; расширить минимальный контракт Access групповыми назначениями teacherId. Проверять существование учреждения и тип/область сотрудника через владельцев; UI учётных записей B2 не является runtime-поставщиком.",
@@ -318 +568 @@
-        "Индексы и версия группы. Подробная итоговая ORG-11 — W33; обычный PATCH не обходит отдельные команды продления."
+        "Индексы и версия группы. Подробная итоговая ORG-11 — N1; обычный PATCH не обходит отдельные команды продления."
@@ -328,4 +578,61 @@
-      "activation": "Подключить готовый редактор структуры в тестовой среде; полные зависимые карточки позднее."
-    },
-    {
-      "id": "W09",
+      "activation": "Подключить готовый редактор структуры в тестовой среде; полные зависимые карточки позднее.",
+      "legacyIds": [
+        "W08"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "C4",
+        "B2",
+        "D1",
+        "E3"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: C2. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции ORG-06, ORG-07, ORG-08, ORG-09, ORG-10, ORG-12; У учреждения есть самостоятельные съёмки/группы; сроки меняет один владелец.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "C4",
+      "title": "Полная карточка учреждения",
+      "kind": "implementation",
+      "owners": [
+        "morefoto.organization",
+        "morefoto.access"
+      ],
+      "dependsOn": [
+        "C3"
+      ],
+      "decisionGates": [
+        "D08"
+      ],
+      "endpointIds": [
+        "ORG-04"
+      ],
+      "verifiesEndpointIds": [],
+      "outcome": "Карточка учреждения имеет полный структурный ответ; отсутствие ещё не подключённого финансового поставщика явно отражено согласованным union.",
+      "scope": [
+        "Полный ORG-04 с реальными реквизитами учреждения, назначениями, съёмками и группами.",
+        "shoots/groups обязательны и независимо пагинируются; один из поставщиков не заменяется пустым массивом.",
+        "Summary использует принятый A6 union: unavailable{reason=dependenciesNotReady} до финансового поставщика; ready только от подключённого владельца, ошибка подключённого поставщика — 503."
+      ],
+      "review": [
+        "Минимальный контракт назначений C2 и структурный поставщик C3 уже в main.",
+        "Воспитателю ORG-04 не открывается; assignmentSignature только организатору; чужая область запрещена."
+      ],
+      "acceptance": [
+        "Список съёмок/групп, независимые страницы, корректная сортировка и разные учреждения.",
+        "До финансового поставщика проверен именно unavailable; ready и итоговые суммы перепроверяются в N1."
+      ],
+      "activation": "Подключить целую структурную карточку; финансовый ready после появления поставщика и повторной проверки N1.",
+      "legacyIds": [
+        "W07"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "N1"
+      ],
+      "inputBoundary": "C3 и его зависимости уже в main: реальные учреждения, institution/group assignments, съёмки и группы. Финансовый provider допускает только согласованное unavailable до подключения.",
+      "outputBoundary": "Завершённые операции ORG-04; Карточка учреждения имеет полный структурный ответ; отсутствие ещё не подключённого финансового поставщика явно отражено согласованным union.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "B2",
@@ -340,3 +647,3 @@
-        "W02",
-        "W06",
-        "W08"
+        "A3",
+        "B1",
+        "C3"
@@ -381,4 +688,15 @@
-      "activation": "Подключить служебные роли и готовые редакторы; полные сводки — после W33."
-    },
-    {
-      "id": "W10",
+      "activation": "Подключить служебные роли и готовые редакторы; полные сводки — после N1.",
+      "legacyIds": [
+        "W09"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "F1",
+        "N1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: A3, B1, C3. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции ACC-02, ACC-03, ACC-04, ACC-05, ACC-06; Управление учётками/областями влияет на действующие сессии.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "D1",
@@ -392,4 +710,2 @@
-        "W03",
-        "W04",
-        "W08",
-        "W09"
+        "A4",
+        "C3"
@@ -411 +727,2 @@
-        "Пределы размера/формата/ресурсов, индексы dedup и метаданные; не возвращать публичный src оригинала."
+        "Пределы размера/формата/ресурсов, индексы dedup и метаданные; не возвращать публичный src оригинала.",
+        "Серверные полномочия и область поступают из B1/C2/C3; панель управления сотрудниками B2 не нужна как отдельный runtime-поставщик."
@@ -421,4 +738,16 @@
-      "activation": "Подключить загрузку/обработку. Галерея — W15, файлы покупателя — W25."
-    },
-    {
-      "id": "W11",
+      "activation": "Подключить загрузку/обработку. Галерея — E4, файлы покупателя — J1.",
+      "legacyIds": [
+        "W10"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "D2",
+        "J1",
+        "L1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: A4, C3. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции MED-02, MED-03, MED-04; Организатор загружает фото в съёмку и видит состояние обработки без публичного оригинала.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "D2",
@@ -431 +760 @@
-        "W10"
+        "D1"
@@ -448 +777 @@
-        "Стабильные ID, атомарная запись списка; перенос MED-07 оставить до заказа W17.",
+        "Стабильные ID, атомарная запись списка; перенос MED-07 оставить до заказа D3.",
@@ -457,7 +786,19 @@
-        "MED-02/04 отражают назначение; регресс с открытой группой и заказом — W18."
-      ],
-      "activation": "Подключить разметку организатора, переносы/продажа закрыты."
-    },
-    {
-      "id": "W12",
-      "title": "Товары каталога",
+        "MED-02/04 отражают назначение; регресс с открытой группой и заказом — F2."
+      ],
+      "activation": "Подключить разметку организатора, переносы/продажа закрыты.",
+      "legacyIds": [
+        "W11"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "F1",
+        "E4",
+        "D3"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: D1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции MED-05, MED-06; Назначение кадров группе/ребёнку и обложка меняются согласованно.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "E2",
+      "title": "Каталог: защищённые HTTP-операции",
@@ -469,6 +810,4 @@
-        "W09",
-        "W11"
-      ],
-      "decisionGates": [
-        "D02"
-      ],
+        "B1",
+        "E1"
+      ],
+      "decisionGates": [],
@@ -481,17 +820,28 @@
-      "outcome": "Каталог имеет серверные единицы/цены/доступность, отдельно от снимка покупки.",
-      "scope": [
-        "Подключить morefoto.commerce: список/создание/изменение товара, цена в минимальных денежных единицах.",
-        "Индексы, ограничения, версия; изменение каталога не переписывает будущую покупку."
-      ],
-      "review": [
-        "Форматы/комплекты/единицы утверждены D02; деньги не float.",
-        "Только разрешённые роли меняют товар, некорректная цена отклоняется."
-      ],
-      "acceptance": [
-        "Создание, смена цены/доступности, stale version и запрещённая роль.",
-        "Контракт согласован с опубликованным редактором."
-      ],
-      "activation": "Подключить редактор каталога, продажи ещё закрыты."
-    },
-    {
-      "id": "W13",
+      "outcome": "Организатор читает и редактирует каталог через COM-01–03 с серверной проверкой доступа.",
+      "scope": [
+        "Добавить COM-01/02/03 поверх готового каталога E1 и политики организатора B1.",
+        "Сохранить реквизиты Product, целую цену, catalogRevision, валидацию и общий конфликт версии.",
+        "Нет зависимости от управления сотрудниками B2 или фотографий D2: эти операции работают только с каталогом. D02 остаётся открытым для ассортимента/условий продаж E3; демо-цены не публикуются как утверждённые."
+      ],
+      "review": [
+        "Организатор допускается, остальные роли/нет профиля/отозванный токен запрещены.",
+        "HTTP не обходит CAS/валидацию внутренних сценариев; product_id проверен."
+      ],
+      "acceptance": [
+        "Реальный HTTP create/list/update, целая цена, неверный товар и stale revision.",
+        "Тест проходит на main+E2 после merge B1/E1 без B2, Media и других незамерженных волн."
+      ],
+      "activation": "Подключить редактор каталога; это не включает продажу, корзину, цены для покупателей или демонстрационный seed.",
+      "legacyIds": [
+        "W12"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "E3"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: B1, E1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции COM-01, COM-02, COM-03; Организатор читает и редактирует каталог через COM-01–03 с серверной проверкой доступа.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "E3",
@@ -505,2 +855,2 @@
-        "W08",
-        "W12"
+        "C3",
+        "E2"
@@ -533,6 +883,18 @@
-        "Согласованные бизнес-примеры расчёта проходят без мока; повтор после открытия — W18/W19."
-      ],
-      "activation": "Подключить редактор условий. Витрина — W15."
-    },
-    {
-      "id": "W14",
+        "Согласованные бизнес-примеры расчёта проходят без мока; повтор после открытия — F2/G1."
+      ],
+      "activation": "Подключить редактор условий. Витрина — E4.",
+      "legacyIds": [
+        "W13"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "F1",
+        "E4",
+        "F2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: C3, E2. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции COM-04, COM-05, COM-06, COM-07; Подарки/скидки/условия вычисляются одной версионированной политикой.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "F1",
@@ -546,3 +908,3 @@
-        "W09",
-        "W11",
-        "W13"
+        "B2",
+        "D2",
+        "E3"
@@ -567 +929 @@
-        "Preview/перенос HND-10/11 — W17; здесь не добавлять внешнюю отправку."
+        "Preview/перенос HND-10/11 — D3; здесь не добавлять внешнюю отправку."
@@ -575,6 +937,18 @@
-        "После W17 проверить transferred/results и запрет редактирования перенесённого."
-      ],
-      "activation": "Подключить служебные списки. Передача ссылок/переносы позднее."
-    },
-    {
-      "id": "W15",
+        "После D3 проверить transferred/results и запрет редактирования перенесённого."
+      ],
+      "activation": "Подключить служебные списки. Передача ссылок/переносы позднее.",
+      "legacyIds": [
+        "W14"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "E4",
+        "D3",
+        "F2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: B2, D2, E3. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции HND-06, HND-07, HND-08, HND-09, HND-12; Служебная заявка и уточнение имеют статус и проверяемое право на скидку.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "E4",
@@ -588,3 +962,3 @@
-        "W11",
-        "W13",
-        "W14"
+        "D2",
+        "E3",
+        "F1"
@@ -608 +982 @@
-        "Capability галереи и внутренний lifecycle выдачи/отзыва; публичная подготовка ссылки — W18.",
+        "Capability галереи и внутренний lifecycle выдачи/отзыва; публичная подготовка ссылки — F2.",
@@ -610 +984 @@
-        "Проверять группу/фото/количество. capabilities.receiptChannels содержит лишь реально подключённые каналы, повтор после W21."
+        "Проверять группу/фото/количество. capabilities.receiptChannels содержит лишь реально подключённые каналы, повтор после G2."
@@ -618,6 +992,17 @@
-        "Изолированный lifecycle capability без бизнес-обхода; реальное открытие проверяется W18."
-      ],
-      "activation": "Проверять только на тестовых данных. Покупательская передача/продажи требуют W18 и W34."
-    },
-    {
-      "id": "W16",
+        "Изолированный lifecycle capability без бизнес-обхода; реальное открытие проверяется F2."
+      ],
+      "activation": "Проверять только на тестовых данных. Покупательская передача/продажи требуют F2 и N2.",
+      "legacyIds": [
+        "W15"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "E5",
+        "F2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: D2, E3, F1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции MED-01, COM-08, COM-09; Покупатель видит разрешённые превью и получает проверяемый сервером quote.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "E5",
@@ -630 +1015 @@
-        "W15"
+        "E4"
@@ -650 +1035 @@
-        "COM-10 требует открытой группы. Обязательные paymentStatus/productionStatus/period и поздние проекции повторно проверяются W18–W33."
+        "COM-10 требует открытой группы. Обязательные paymentStatus/productionStatus/period и поздние проекции повторно проверяются F2–N1."
@@ -661,4 +1046,17 @@
-      "activation": "Оформление только на изолированном стенде без списаний. Полные карточки нельзя переключать до W33."
-    },
-    {
-      "id": "W17",
+      "activation": "Оформление только на изолированном стенде без списаний. Полные карточки нельзя переключать до N1.",
+      "legacyIds": [
+        "W16"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "D3",
+        "G1",
+        "I1",
+        "J1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: E4. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции COM-10, COM-11, COM-12, COM-13; Оформление фиксирует quote и неизменяемый заказ; личный ключ отделён от галереи.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "D3",
@@ -673,3 +1071,3 @@
-        "W11",
-        "W14",
-        "W16"
+        "D2",
+        "F1",
+        "E5"
@@ -708,6 +1106,17 @@
-        "Повтор после сбоя, MED-02/04 и HND-08/09 отражают итог; повтор с оплатой/возвратом — W24."
-      ],
-      "activation": "Включить переносы после снимка заказа. Платёжные последствия перепроверить W24."
-    },
-    {
-      "id": "W18",
+        "Повтор после сбоя, MED-02/04 и HND-08/09 отражают итог; повтор с оплатой/возвратом — I3."
+      ],
+      "activation": "Включить переносы после снимка заказа. Платёжные последствия перепроверить I3.",
+      "legacyIds": [
+        "W17"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "F2",
+        "I3"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: D2, F1, E5. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции MED-07, HND-10, HND-11; Перенос сохраняет ID/покупку и не создаёт дубли или накопление скидок.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "F2",
@@ -722,4 +1131,4 @@
-        "W13",
-        "W14",
-        "W15",
-        "W17"
+        "E3",
+        "F1",
+        "E4",
+        "D3"
@@ -755 +1164 @@
-        "HND-03 проверяет фото/назначения/условия/списки; lifecycle capability использует W15.",
+        "HND-03 проверяет фото/назначения/условия/списки; lifecycle capability использует E4.",
@@ -767,4 +1176,16 @@
-      "activation": "Подключить в тестовой среде. Реальных покупателей принимать после W34."
-    },
-    {
-      "id": "W19",
+      "activation": "Подключить в тестовой среде. Реальных покупателей принимать после N2.",
+      "legacyIds": [
+        "W18"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "G1",
+        "L1",
+        "K2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: E3, F1, E4, D3. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции HND-01, HND-02, HND-03, HND-04, HND-05; Ссылка выдаётся после готовности, сроки считаются от записанного факта передачи.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "G1",
@@ -778,2 +1199,2 @@
-        "W16",
-        "W18"
+        "E5",
+        "F2"
@@ -803 +1224 @@
-        "Денежный факт отделить от исполнения; поздний платёж сохраняется, но не открывает файл/печать до W24. PAY-07 уточнить после D09."
+        "Денежный факт отделить от исполнения; поздний платёж сохраняется, но не открывает файл/печать до I3. PAY-07 уточнить после D09."
@@ -813,4 +1234,17 @@
-      "activation": "Только sandbox. Реальные списания закрыты до возвратов, исполнения, выдачи и W34."
-    },
-    {
-      "id": "W20",
+      "activation": "Только sandbox. Реальные списания закрыты до возвратов, исполнения, выдачи и N2.",
+      "legacyIds": [
+        "W19"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "G2",
+        "I1",
+        "I2",
+        "I3"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: E5, F2. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции PAY-01, PAY-02, PAY-03, PAY-07; Попытка, webhook и неизвестный исход обрабатываются без ложного успешного заказа.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "H1",
@@ -824,3 +1258,2 @@
-        "W01",
-        "W04",
-        "W19"
+        "A2",
+        "A5"
@@ -839 +1272,2 @@
-        "Потребитель выбирает получателя/разрешение/смысл. Notification выполняет транспорт и сообщает наблюдаемый статус; Lead/LeadHunter не переподключать."
+        "Потребитель выбирает получателя/разрешение/смысл. Notification выполняет транспорт и сообщает наблюдаемый статус; Lead/LeadHunter не переподключать.",
+        "Завершённый внутренний сценарий доставки и проверка выбранного канала не импортируют Payments: интеграция с чеком принадлежит G2. D09 остаётся открытым блокером канала."
@@ -851,4 +1285,15 @@
-      "activation": "Включить в тестовой среде; первый продуктовый клиент W21. Нового REST у Notification нет."
-    },
-    {
-      "id": "W21",
+      "activation": "Включить в тестовой среде; первый продуктовый клиент G2. Нового REST у Notification нет.",
+      "legacyIds": [
+        "W20"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "G2",
+        "J2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: A2, A5. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Без новых публичных HTTP-операций; Канал первого потребителя имеет внутренний контракт, сохраняемые попытки и управляемые повторы.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "G2",
@@ -862,2 +1307,2 @@
-        "W19",
-        "W20"
+        "G1",
+        "H1"
@@ -892,4 +1337,14 @@
-      "activation": "Чеки только на тестовых адресах/в sandbox до W34."
-    },
-    {
-      "id": "W22",
+      "activation": "Чеки только на тестовых адресах/в sandbox до N2.",
+      "legacyIds": [
+        "W21"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "I1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: G1, H1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции PAY-04, PAY-05, PAY-06, PAY-09; Payments владеет фискальным чеком, Notification — исполнением отправки.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "I1",
@@ -903,3 +1358,3 @@
-        "W16",
-        "W19",
-        "W21"
+        "E5",
+        "G1",
+        "G2"
@@ -926 +1381 @@
-        "Итоги по D06, включая полностью возвращённые заказы. Возвраты/решения в SET-01 допроверяются W23/W24."
+        "Итоги по D06, включая полностью возвращённые заказы. Возвраты/решения в SET-01 допроверяются I2/I3."
@@ -936,4 +1391,16 @@
-      "activation": "Подключить разрешённые исправления на стенде. Возвраты/исполнение — W23/W24."
-    },
-    {
-      "id": "W23",
+      "activation": "Подключить разрешённые исправления на стенде. Возвраты/исполнение — I2/I3.",
+      "legacyIds": [
+        "W22"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "I2",
+        "I3",
+        "N1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: E5, G1, G2. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции SET-01, SET-02, SET-03; Исправление добавляет историю/версию, сохраняя исходную покупку и платёжный факт.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "I2",
@@ -947,2 +1414,2 @@
-        "W19",
-        "W22"
+        "G1",
+        "I1"
@@ -977,4 +1444,15 @@
-      "activation": "Только sandbox, затем решения W24 и финальный W34."
-    },
-    {
-      "id": "W24",
+      "activation": "Только sandbox, затем решения I3 и финальный N2.",
+      "legacyIds": [
+        "W23"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "I3",
+        "N1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: G1, I1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции SET-04, SET-05, SET-06, PAY-08; Конкурентные запросы не возвращают больше остатка, unknown не вызывает второе списание.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "I3",
@@ -989,4 +1467,4 @@
-        "W17",
-        "W19",
-        "W22",
-        "W23"
+        "D3",
+        "G1",
+        "I1",
+        "I2"
@@ -1025,6 +1503,21 @@
-        "Повтор переносов купленного и чтения заказа после исправления/возврата; полный эффект проверяется W25/W29–W32."
-      ],
-      "activation": "Подключить решения на стенде. Реальное исполнение ждёт проверки Files/Production."
-    },
-    {
-      "id": "W25",
+        "Повтор переносов купленного и чтения заказа после исправления/возврата; полный эффект проверяется J1/L1–M1."
+      ],
+      "activation": "Подключить решения на стенде. Реальное исполнение ждёт проверки Files/Production.",
+      "legacyIds": [
+        "W24"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "J1",
+        "J3",
+        "K1",
+        "L1",
+        "M1",
+        "N1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: D3, G1, I1, I2. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции SET-07; Решение отдельно задаёт права на файлы и разрешение печати с учётом оплаты, исправлений и возврата.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "J1",
@@ -1039,3 +1532,3 @@
-        "W10",
-        "W16",
-        "W24"
+        "D1",
+        "E5",
+        "I3"
@@ -1073,4 +1566,15 @@
-      "activation": "Подключить файлы в sandbox. Реальные продажи — после W34."
-    },
-    {
-      "id": "W26",
+      "activation": "Подключить файлы в sandbox. Реальные продажи — после N2.",
+      "legacyIds": [
+        "W25"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "J2",
+        "J3"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: D1, E5, I3. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции FIL-01, FIL-02, FIL-03, FIL-04; Покупатель получает только разрешённые оплаченные оригиналы в пределах срока, даже из готового архива.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "J2",
@@ -1084,2 +1588,2 @@
-        "W20",
-        "W25"
+        "H1",
+        "J1"
@@ -1111,4 +1615,14 @@
-      "activation": "Только тестовые получатели до W34."
-    },
-    {
-      "id": "W27",
+      "activation": "Только тестовые получатели до N2.",
+      "legacyIds": [
+        "W26"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "J3"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: H1, J1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции FIL-05, FIL-06; Запрос письма и его статус связаны с конкретным заказом и операцией.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "J3",
@@ -1122,3 +1636,3 @@
-        "W24",
-        "W25",
-        "W26"
+        "I3",
+        "J1",
+        "J2"
@@ -1150,4 +1664,14 @@
-      "activation": "Подключить действие куратора после обычной выдачи и писем."
-    },
-    {
-      "id": "W28",
+      "activation": "Подключить действие куратора после обычной выдачи и писем.",
+      "legacyIds": [
+        "W27"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "K1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: I3, J1, J2. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции FIL-07, FIL-08; Куратор восстанавливает выдачу по разрешённому действию с сохранением исходного срока доступа и историей.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "K1",
@@ -1160,2 +1684,2 @@
-        "W24",
-        "W27"
+        "I3",
+        "J3"
@@ -1179 +1703 @@
-        "Безопасная проекция заказа/сопровождения; SUP-06 оставить после печатных версий W31.",
+        "Безопасная проекция заказа/сопровождения; SUP-06 оставить после печатных версий K2.",
@@ -1188,6 +1712,17 @@
-        "CaseItem.order после печати/доставки повторить W33; выбранный канал проверяется отдельным адаптером."
-      ],
-      "activation": "Подключить обращения/историю на стенде; продление — W31."
-    },
-    {
-      "id": "W29",
+        "CaseItem.order после печати/доставки повторить N1; выбранный канал проверяется отдельным адаптером."
+      ],
+      "activation": "Подключить обращения/историю на стенде; продление — K2.",
+      "legacyIds": [
+        "W28"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "L1",
+        "K2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: I3, J3. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции SUP-01, SUP-02, SUP-03, SUP-04, SUP-05; Покупатель и куратор работают с одним обращением с разной видимостью данных.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "L1",
@@ -1200,4 +1735,4 @@
-        "W10",
-        "W18",
-        "W24",
-        "W28"
+        "D1",
+        "F2",
+        "I3",
+        "K1"
@@ -1241,4 +1776,14 @@
-      "activation": "Подключить подготовку и тестовый экспорт. Физическую печать автоматически не запускать."
-    },
-    {
-      "id": "W30",
+      "activation": "Подключить подготовку и тестовый экспорт. Физическую печать автоматически не запускать.",
+      "legacyIds": [
+        "W29"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "L2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: D1, F2, I3, K1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции PRD-01, PRD-02, PRD-03, PRD-04, PRD-07; Задание — неизменяемый снимок разрешённого состава с проверяемой версией.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "L2",
@@ -1251 +1796 @@
-        "W29"
+        "L1"
@@ -1286,4 +1831,15 @@
-      "activation": "Подключить служебные отметки на тестовых данных."
-    },
-    {
-      "id": "W31",
+      "activation": "Подключить служебные отметки на тестовых данных.",
+      "legacyIds": [
+        "W30"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "K2",
+        "M1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: L1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции PRD-05, PRD-06; Старт и комплектация относятся к конкретной версии и конкретному заказу.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "K2",
@@ -1298,3 +1854,3 @@
-        "W18",
-        "W28",
-        "W30"
+        "F2",
+        "K1",
+        "L2"
@@ -1340,4 +1896,14 @@
-      "activation": "Подключить продление после печати; Shipping ready перепроверяется W32."
-    },
-    {
-      "id": "W32",
+      "activation": "Подключить продление после печати; Shipping ready перепроверяется M1.",
+      "legacyIds": [
+        "W31"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "M1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: F2, K1, L2. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции SUP-06; Продление не оставляет устаревшую печатную версию или ложную готовность.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "M1",
@@ -1351,3 +1917,3 @@
-        "W24",
-        "W30",
-        "W31"
+        "I3",
+        "L2",
+        "K2"
@@ -1390,4 +1956,14 @@
-      "activation": "Подключить отметки на стенде. Сквозной выпуск — W34."
-    },
-    {
-      "id": "W33",
+      "activation": "Подключить отметки на стенде. Сквозной выпуск — N2.",
+      "legacyIds": [
+        "W32"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "N1"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: I3, L2, K2. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции SHP-01, SHP-02, SHP-03, SHP-04, SHP-05; Передаётся актуальная партия, история отделена от текущей готовности.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "N1",
@@ -1402,5 +1978,6 @@
-        "W09",
-        "W22",
-        "W23",
-        "W24",
-        "W32"
+        "B2",
+        "C4",
+        "I1",
+        "I2",
+        "I3",
+        "M1"
@@ -1445 +2022 @@
-        "Финансы из Settlement, сроки из Organization, назначения из Access, исполнение от владельцев; ORG-04 summary по форме W05.",
+        "Финансы из Settlement, сроки из Organization, назначения из Access, исполнение от владельцев; ORG-04 summary по форме A6.",
@@ -1458,4 +2035,14 @@
-      "activation": "Переключить целые ролевые экраны с полными ответами; ранние частичные подключения закрываются этой проверкой."
-    },
-    {
-      "id": "W34",
+      "activation": "Переключить целые ролевые экраны с полными ответами; ранние частичные подключения закрываются этой проверкой.",
+      "legacyIds": [
+        "W33"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [
+        "N2"
+      ],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: B2, C4, I1, I2, I3, M1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Завершённые операции ORG-01, ORG-11; Карточки показывают все обещанные реальные поля и соблюдают видимость учреждения.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
+    },
+    {
+      "id": "N2",
@@ -1482 +2069 @@
-        "W33"
+        "N1"
@@ -1616 +2203,9 @@
-      "activation": "После приёмки можно согласовать production-выпуск. Сам запуск не входит в подготовку этого плана."
+      "activation": "После приёмки можно согласовать production-выпуск. Сам запуск не входит в подготовку этого плана.",
+      "legacyIds": [
+        "W34"
+      ],
+      "deliveryState": "planned",
+      "unlocks": [],
+      "inputBoundary": "Установленные и принятые в main контракты/схема: N1. Открытые decisionGates проверяются отдельно до начала зависимого участка.",
+      "outputBoundary": "Без новых публичных HTTP-операций; Подтверждена готовность функций и известны восстановление, включение и пределы гарантий.",
+      "mergeCheck": "Отдельный PR к актуальному main; все dependsOn уже merged. Проверки на main + только этот diff, без файлов/коммитов незамерженных волн; установить миграции и DI своего сценария. Повторить проверку после обновления base."
diff --git a/docs/04-bitrix-modules/render-waves.py b/docs/04-bitrix-modules/render-waves.py
--- a/docs/04-bitrix-modules/render-waves.py
+++ b/docs/04-bitrix-modules/render-waves.py
@@ -2 +2 @@
-"""Render the reviewed wave plan; business/API source remains in backend-waves.json/build.py."""
+"""Render the independent wave DAG; endpoint contracts remain in build.py."""
@@ -4,0 +5,2 @@
+from wave_graph import validate_graph
+
@@ -7 +9,2 @@
-intro = """# План подключения бэкенда MoreFoto по волнам
+state = validate_graph(plan)
+intro = """# Независимые волны MoreFoto
@@ -9 +12 @@
-Дата: **11 сентября 2026 года**. Сервер — **RaBit API**. Статус: **план реализации, 35 последовательных волн W00–W34; ни одна волна этим документом не объявлена выполненной**. R17 и D01–D12 сохраняют свой фактический статус. Работающие Lead/LeadHunter сохранены; продуктовые morefoto.* ещё не подключены.
+Дата: **11 сентября 2026 года**. Сервер — **RaBit API**. Активный план — граф зависимостей с буквами направлений и номерами срезов. Буквы не задают порядок выполнения. Статусы ниже — снимок на указанном main; перед стартом/merge проверить GitHub заново. Переименование не объявляет HTTP реализованным.
@@ -11 +14 @@
-[Карта модулей и правила](README.md) · [REST-контракты](../05-rest-api/README.md) · [Postman по волнам](../05-rest-api/postman/MoreFoto.backend-waves.postman_collection.json) · [Postman по модулям](../05-rest-api/postman/MoreFoto.postman_collection.json) · [Решения D01–D12](../frontend/business-review/decisions.md).
+[Карта модулей](README.md) · [REST-контракты](../05-rest-api/README.md) · [Postman по волнам](../05-rest-api/postman/MoreFoto.backend-waves.postman_collection.json) · [Решения D01–D12](../frontend/business-review/decisions.md).
@@ -13 +16 @@
-## Как работать по этому плану
+## Обязательное правило независимости
@@ -15 +18 @@
-Одна волна — одна проверяемая ответственность, обычно один PR. Внутри неё вместе идут нужные таблицы/индексы и миграции, Repository, UseCase/DTO, DI/routes, тесты и описание поведения. Не выносить все DTO, все миграции или все тесты отдельными техническими выпусками: ревьюер должен увидеть работающий сценарий целиком.
+Каждая волна — один завершённый сценарий, одна ветка `codex/<буква><номер>-...` от актуального `main` и один PR в `main`. Все необходимые runtime, compile и schema зависимости к началу работы уже слиты в main. Draft PR, проверенная ветка и обещанный интерфейс не закрывают зависимость. Не создавать stacked PR, не переносить код незамерженной соседней волны и не подменять поставщика allow-all, mock или фиктивным fallback.
@@ -17 +20 @@
-Порядок последовательный: следующая волна принимается после предыдущей и её указанных зависимостей. Сначала W00–W04 исправляют проверенные ограничения существующей основы; W05 фиксирует бизнес/домен/схему; W06–W33 добавляют продуктовые сценарии; W34 проверяет весь путь. 11 сентября 2026 пользователь принял модель W05 и разрешил реализацию W06–W09 по рекомендациям. Проверка интерфейса R17 остаётся условием выпуска; несущественные настройки уточняются по ходу, большие изменения модели согласовываются заранее.
+Берём одновременно несколько готовых волн. Заблокированные пропускаем до слияния их зависимостей и решения затрагивающих их вопросов. `dependsOn` — обязательные предшественники; `unlocks` — прямые потребители. Merge одного предшественника разрешает потребителя только при выполнении **всех** его условий. Повторные проверки (`verificationWaves`) — отдельные проверки контрактов после развития соседних владельцев, а не обратные runtime-зависимости.
@@ -19 +22 @@
-**Ориентир 3–4 тысячи строк — сигнал проверить размер, не квота.** Считать прежде всего рукописный production-код, миграции и значимые тесты. Генерируемые коллекции/реестры, lock-файлы и форматирование учитывать отдельно при оценке ревью, но не прятать из diff. Маленькую волну не раздувать. Если два независимых предмета или слишком большой diff — заранее разделить её на A/B, указать новую зависимость и обновить JSON/карты. ID остальных волн не сдвигать. Например, W10 может разделиться на приём оригинала и обработку превью; MED-03 считается готовым после обоих. W19 (платёжная надёжность) и W20 (транспорт) также требуют проверки размера до кодирования, но не дробления по папкам.
+Если зависимый сценарий велик, выделить самостоятельный работающий внутренний срез с хранением, UseCase, DI и тестами. Нельзя выдавать набор DTO/миграций за готовую волну. Его вход и выход должны иметь смысл без следующего PR. Так C1 реализует внутренние учреждения, E1 — внутренний каталог; защищённый HTTP появляется в C2/E2 после B1. У этих внутренних срезов нет публичных маршрутов и заявления о готовом frontend.
@@ -21 +24 @@
-### Реализация, проверка и подключение — разные отметки
+В каждой волне проверить установку/миграции и значимые сценарии на **актуальном main плюс только её diff**, без соседних веток. После изменения base повторить затронутые проверки; совместная проверка нескольких веток дополняет, но не заменяет отдельную. Общие изменения bootstrap, маршрутов и Composer добавляются в первом действительно требующем их срезе, не в независимых заготовках. Merge и deployment — отдельные действия после ревью.
@@ -23,3 +26 @@
-- `implementationWave` в реестре/Postman — где реализуется основная ответственность endpoint. Для существующих методов это инвентаризация или исправление, не разработка заново.
-- `verificationWaves` — где его нужно повторно проверить после появления смежных владельцев данных. Это не обратная зависимость реализации.
-- **Подключение** в карточке волны — допустимый уровень использования: изолированный стенд, готовый сценарий frontend или финальный допуск к внешним эффектам. Наличие метода/пути не означает готовность полного ответа.
+Рукописные 3–4 тысячи строк — сигнал перепроверить размер, не квота. Не раздувать маленькую волну; генерируемые файлы учитывать отдельно, но не скрывать из ревью. У каждого API ID ровно один первичный владелец; при разделении уже выданные ID других волн не сдвигать.
@@ -27 +28 @@
-Реальные `unpaid`, `not-started`, отсутствие назначения и пустой список допустимы для соответствующего состояния данных. Если обязательное поле ещё нельзя получить от его владельца, контракт остаётся неподключённым; `0`, `null`, `{}` или `[]` не маскируют отсутствующий модуль. Финальные карточки заказа/обращения и сводки завершаются в W33. Для каждого переключаемого экрана проверить весь набор его запросов, запись и последующее чтение; не смешивать реальные изменения со скрытым fallback на моки.
+## Текущий независимый набор
@@ -29 +30 @@
-Волны Postman — навигация для ревью, **не сценарий автоматического Run collection**. Внутри папки порядок ID не определяет последовательность действий и тестовые данные. Настраивать отдельный стенд/окружение, выбирать необходимые запросы вручную. SHR-01/NTF-01 блокируются по умолчанию через `allow_platform_requests`; явное разрешение нужно только для осознанной проверки upload/заявки на тестовом адресате. Offline-валидаторы не отправляют HTTP и не подтверждают работу PHP/БД/Telegram.
+A1–A6 (W00–W05) уже в main. B1 (W06) находится в [draft PR #8](https://github.com/rebit-pro/rabit-api/pull/8) и пока не удовлетворяет зависимости. **C1 и E1 взяты в работу независимо от B1 и друг от друга.** A7 фиксирует это правило и не является runtime-зависимостью остальных. Следующие C2/E2 ожидают merge B1 и соответствующего внутреннего среза.
@@ -31 +32 @@
-## Когда впервые подключается модуль
+Пользователь принял модель Access/Organization, одного продавца, связь фото с несколькими детьми и рекомендации D07/D08/D10. R17 и остальные открытые решения не объявлены закрытыми. D02 не мешает пустому редактируемому каталогу, но продолжает блокировать соответствующие коммерческие правила E3. D09 продолжает блокировать выбор платёжного/доставочного канала. GitHub Actions отключён вручную; YAML сохранён, проверки выполняются локально.
@@ -33,61 +34 @@
-| Модуль | Подключение | Последующее развитие |
-| --- | --- | --- |
-| rebit.share | Уже в основе; W01/W03/W04 исправляют ограничения | Общие узкие контракты добавляются с первым потребителем |
-| rebit.auth | Уже в основе; W02 — исправление сессии | W06/W09 — интеграция с полномочиями сотрудников |
-| rebit.notification | Уже есть Lead, NTF-01 в W00 | W20 — внутренний транспорт; W21 — чеки, W26 — файлы |
-| rebit.leadhunter | Уже есть CLI/cron, проверка W00 | Сохранить независимую работающую лидогенерацию |
-| morefoto.access | W06 | W09 — управление назначениями, W33 — ролевые проекции |
-| morefoto.organization | W07 | W08 — структура, W31 — продление, W33 — полные карточки |
-| morefoto.media | W10 | W11 — разметка, W15 — галерея, W17 — перенос |
-| morefoto.commerce | W12 | W13 — расчёт, W15 — quote, W16 — снимок заказа |
-| morefoto.handoff | W14 | W17 — перенос, W18 — ссылка и сроки |
-| morefoto.payments | W19 | W21 — чеки, W23 — внешняя часть возврата |
-| morefoto.settlement | W22 | W23 — резервы, W24 — решение исполнения |
-| morefoto.files | W25 | W26 — доставка, W27 — восстановление |
-| morefoto.support | W28 | W31 — продление с перепроверкой производства |
-| morefoto.production | W29 | W30 — старт/комплектация, W31/W32 — актуальность версии |
-| morefoto.shipping | W32 | W33/W34 — сводки и сквозной регресс |
-
-В первом PR модуля добавить его реальный вертикальный срез, установку/миграции, DI, bootstrap/autoload и регистрацию routes.php в `api/public/local/routes/rabit-api.php`. Проверить эффективный `local/.settings_extra.php → php_interface/settings_extra.php`. Установка/симлинк routes.php сами по себе не изменяют фиксированный реестр. Новые миграции должны быть видны активному конфигу Sprint; прежние семь foundation-версий и таблица истории сохраняются, исторический каталог P2P не включается. Отключение функции/откат кода согласовать с реестром, миграцией и consumer; удаление данных не считать штатным откатом.
-
-## Notification в MoreFoto и карте API
-
-Общий реестр содержит **99 операций: исходные 97 плюс 2 платформенных**. Из исходных 97: 89 проектных операций MoreFoto, 3 эскиза webhook, 2 используемых Auth, 2 унаследованных регистрации и 1 frontend health. Добавлены только существующие **SHR-01 POST /share/file/upload/** и **NTF-01 POST /lead**; префикс backend `/api/v1`. Эти методы не становятся продуктовыми требованиями MoreFoto. Наличие исходников отмечается отдельно от live-проверки. LeadHunter HTTP-методов не добавляет.
-
-| Сценарий | REST-владелец | Роль Notification |
-| --- | --- | --- |
-| Заявка с внешнего сайта | rebit.notification, NTF-01 | Существующая синхронная доставка в Telegram, условный email fallback |
-| Чек и повтор его отправки | morefoto.payments, PAY-04–PAY-06/PAY-09 | W20/W21: исполнение доставки; фискальный факт остаётся Payments |
-| Письмо с файлами и восстановление | morefoto.files, FIL-05–FIL-08 | W26/W27: доставка; право/срок оригинала остаётся Files |
-| Ответ куратора | morefoto.support, SUP-05 | Только если выбран внешний канал; хранение ответа уже самостоятельный сценарий |
-| Передача ссылки / партии | Handoff / Shipping | HND-04 и SHP-04 записывают ручной/физический факт, не подразумевают отправку |
-
-Не добавлять вымышленные `/notifications`, inbox, настройки каналов или просмотр прочтений. Старые торговые enum/очередь не возвращаются. Для нового транспорта проверить поддерживаемые каналы, повтор/дедупликацию, неизвестный исход и разницу между принятием транспортом и доставкой. W20 не переписывает действующие Lead/LeadHunter в универсальную очередь.
-
-## Находки аудита и волны исправлений
-
-| Находка исходников | Волна | Чем доказать исправление |
-| --- | --- | --- |
-| LoggerFilter записывает чувствительные поля | W01 | Секрет не появляется ни в успешном, ни в ошибочном логе |
-| Не проверяется ACTIVE; pending и disabled смешаны | W02 | Заблокированный пользователь не получает доступ/реактивацию |
-| UF_TOKEN_EXPIRES_AT string не читается как DateTime | W02 | Реальный UF, истёкший/невалидный срок и граница now |
-| Multipart mapper вызывает отсутствующий enableAnnotationMapping; ошибки файла могут стать 500 | W03 | Реальный mapper и негативные HTTP-сценарии SHR/NTF |
-| Владение файлом хранится через неверный lifecycle кеша | W03 | Очистка кеша/рестарт не теряет владельца; чужой ID запрещён |
-| Optional secret resolver смешивает stdout path и лог | W04 | Отсутствующий необязательный секрет обрабатывается без неверного пути |
-| Nginx CORS не разрешает PATCH из продуктовой карты | W04 | OPTIONS и PATCH через реальный proxy с нужными заголовками |
-| Макеты не доказывают денежные/версионные/ролевые гарантии | W06–W34 | Тесты гонок, возвратов, ключей, сроков и полного исполнения |
-
-Подробный исходный аудит — [RaBit API](../../../rebit-p2p/docs/audit-rabit-api.md). БД и production этим документом не проверены. Обнаруженные дефекты перечислены как работа будущих волн, не как уже закрытые проблемы.
-
-## Общий пакет ревью каждой волны
-
-1. Конкретный результат и граница: что теперь делает пользователь/система, какие инварианты сохраняются; ссылка на закрытые решения D и актуальную domain/database-спецификацию.
-2. Миграции/индексы/уникальность/версии и владелец записи. Репозиторий читает SQL/ORM Result как массивы; новый код не гидрирует Objectify-коллекции. ReadModel не выходит за непосредственный маппер в API/контракт/кеш.
-3. Method/path/action/RequestDto/InputDto/OutputDto, роль/область всех ID, DI и эффективный router. Общие технические контракты — `rebit.share/lib/Application/Contract/`, предметные межмодульные — `rebit.share/lib/Contracts/<Domain>/`; interface-key с ручной сборкой — `constructor`. ServiceLocator только bootstrap/DI.
-4. Значимые проверки: happy path, запрещённая роль/чужой ID, неверные поля, stale version, повтор/гонка, сбой зависимости и восстановление — по применимости. Для денег проверять реальную транзакционную БД, для файла реальное ограничение доступа, для HTTP реальные mapper/proxy. Не ограничиваться тестами, повторяющими реализацию.
-5. Проверки изменённого PHP и требуемые проверки репозитория, миграции на тестовой БД, генерация/валидация карты/Postman. Без массового форматирования постороннего кода.
-6. Доказательство выхода: выполненные команды и результаты, оставшиеся ограничения, путь отката/включения и список целых frontend-сценариев, которые можно подключить. Offline fixtures отдельно от живого HTTP и доставки.
-
-Если волна требует открытого решения, её подготовленная схема/контракт остаётся проектом до подтверждения. Дополнительный scope оформляется отдельной волной, а не дописывается в уже принятую незаметно. Для W34 все необходимые продукту решения должны быть подтверждены; визуальная приёмка D01 не задаёт автоматически серверные правила.
-
-## Очередь волн
+## Направления
@@ -96 +37,10 @@
-lines=[intro,"| Волна | Ответственность | Первичные ID | Зависимости | Решения |","| --- | --- | --- | --- | --- |"]
+lines = [intro, "| Буква | Направление |", "| --- | --- |"]
+for letter, title in plan["directions"].items():
+    lines.append(f"| {letter} | {title} |")
+lines += ["", "## Соответствие прежним WNN", "", "Исторические отчёты W00–W06 сохраняют свои имена. Снимок W00 не переписывается. Если WNN разделена, готовность старого объёма требует всех указанных новых срезов.", "", "| Прежняя волна | Новые ссылки |", "| --- | --- |"]
+for index in range(35):
+    legacy = f"W{index:02}"
+    links = [f'[{wave["id"]}](#{wave["id"].lower()})' for wave in plan["waves"] if legacy in wave["legacyIds"]]
+    lines.append(f'| {legacy} | {", ".join(links)} |')
+lines += ["| — | [A7](#a7) — новое правило работы |", "", "## Карта зависимостей", "", "Диаграмма показывает направления ближайших шагов; полный DAG задан таблицей и JSON. Стрелка — необходимая зависимость, а не разрешение немедленно начать.", "", "```mermaid", "graph LR", '  Main["main: A1–A6"] --> B1["B1 Access · PR #8"]', '  Main --> C1["C1 Учреждения core"]', '  Main --> E1["E1 Каталог core"]', '  B1 --> C2["C2 Учреждения HTTP"]', '  C1 --> C2', '  B1 --> E2["E2 Каталог HTTP"]', '  E1 --> E2', '  C2 --> C3["C3 Съёмки и группы"]', '  C3 --> C4["C4 Карточка учреждения"]', '  C3 --> B2["B2 Сотрудники"]', '  C3 --> D1["D1 Приём оригиналов"]', '  C3 --> E3["E3 Условия продаж"]', '  E2 --> E3', "```", "", "| Волна | Ответственность | После merge | Разблокирует* | Первичные ID | Статус |", "| --- | --- | --- | --- | --- | --- |"]
+labels = {"merged": "в main", "review": "на ревью", "inProgress": "в работе", "planned": "запланирована"}
@@ -98,4 +48,2 @@
-    primary=", ".join(wave["endpointIds"]) or "Без новых REST"
-    deps=", ".join(wave["dependsOn"]) or "—"
-    gates=", ".join(wave["decisionGates"]) or "—"
-    lines.append(f'| [{wave["id"]}](#{wave["id"].lower()}) | {wave["title"]} | {primary} | {deps} | {gates} |')
+    lines.append(f'| [{wave["id"]}](#{wave["id"].lower()}) | {wave["title"]} | {", ".join(wave["dependsOn"]) or "—"} | {", ".join(wave["unlocks"]) or "—"} | {", ".join(wave["endpointIds"]) or "Без новых REST"} | {labels[wave["deliveryState"]]} |')
+lines += ["", "*Разблокирует при выполнении остальных зависимостей и decisionGates. Статус «запланирована» не означает готовность к старту.", "", "## Правила API и приёмки", "", "- Реализация, проверка HTTP и подключение целого frontend-сценария отмечаются отдельно. Обязательный неподготовленный поставщик не маскируется нулём или пустым списком.", "- ORG-04 принадлежит C4 и требует реальных shoots/groups из C3. Финансовый summary допускает согласованный unavailable до поставщика; ошибка подключённого поставщика даёт 503. Полный финансовый ready перепроверяется в N1.", "- Новые миграции идут в активный foundation; исторические миграции не менять. SQL/ORM возвращает Result и массивные строки; Objectify-коллекции для нового чтения не применять.", "- Общие технические контракты — rebit.share/lib/Application/Contract; предметные — rebit.share/lib/Contracts. Реализация и DI принадлежат поставщику; ServiceLocator только в bootstrap/DI, interface-key с ручной сборкой использует constructor.", "- Postman — каталог ручных сценариев, не Run all. Offline fixtures не отправляют HTTP и не подтверждают PHP/БД, оплату или доставку. SHR-01/NTF-01 по умолчанию защищены от случайного запуска.", "- Notification сохраняет действующие Lead/LeadHunter; H1 — один подтверждённый транспорт, G2 — интеграция чеков. Не вводить вымышленные inbox/preferences или универсальную IAM.", ""]
@@ -103,4 +51,6 @@
-    lines += ["", f'<a id="{wave["id"].lower()}"></a>', f'## {wave["id"]}. {wave["title"]}', "", wave["outcome"], "",
-      '**Владельцы:** '+", ".join('`'+m+'`' for m in wave["owners"])+'.',
-      '**После:** '+(", ".join(wave["dependsOn"]) or "начало плана")+'. **Решения:** '+(", ".join(wave["decisionGates"]) or "нет новых бизнес-решений")+'.',
-      '**Первичные REST:** '+(", ".join(wave["endpointIds"]) or "нет новых REST")+'.']
+    lines += [f'<a id="{wave["id"].lower()}"></a>', f'## {wave["id"]}. {wave["title"]}', "", wave["outcome"], "",
+              '**Владельцы:** '+", ".join('`'+owner+'`' for owner in wave["owners"])+'.',
+              '**Зависит от:** '+(", ".join(wave["dependsOn"]) or "нет")+'. **Разблокирует:** '+(", ".join(wave["unlocks"]) or "нет")+'.',
+              '**Решения:** '+(", ".join(wave["decisionGates"]) or "нет новых бизнес-решений")+'. **Статус:** '+labels[wave["deliveryState"]]+'.',
+              '**Первичные REST:** '+(", ".join(wave["endpointIds"]) or "нет новых REST")+'.',
+              '**Вход:** '+wave["inputBoundary"], '**Выход:** '+wave["outputBoundary"], '**Независимый merge:** '+wave["mergeCheck"]]
@@ -108 +58 @@
-        verify="все 99 операций реестра, с разделением platform/frontend/product" if wave["id"]=="W34" else ", ".join(wave["verifiesEndpointIds"])
+        verify = "все 99 операций реестра" if wave["id"] == "N2" else ", ".join(wave["verifiesEndpointIds"])
@@ -110,6 +60,6 @@
-    for title,key in [("Входит в волну","scope"),("Предмет ревью","review"),("Доказательство готовности","acceptance")]:
-        lines += ["",f'**{title}:**',""] + ['- '+text for text in wave[key]]
-    lines += ["",'**Подключение:** '+wave["activation"]]
-lines += ["", "## Поддержание плана", "", "Единый источник очереди, зависимостей и соответствия ID — [backend-waves.json](backend-waves.json). Тела HTTP-контрактов остаются в [build.py](../05-rest-api/build.py). Этот Markdown строится [render-waves.py](render-waves.py); сначала изменить JSON, затем выполнить из корня MoreFoto:", "", "```sh", "python3 docs/04-bitrix-modules/render-waves.py", "python3 docs/05-rest-api/build.py", "python3 docs/05-rest-api/validate.py", "node docs/05-rest-api/validate-postman.cjs", "```", "", "Вторая коллекция содержит те же запросы и тесты, что основная, сгруппированные по implementationWave. Волны без REST представлены описанием; повторные проверки перечислены в карточках, не дублируют запросы. Проверки требуют 99 уникальных ID, ровно одну первичную волну на каждый, существующие зависимости без циклов и соответствие коллекций. Команды не подтверждают live-интеграцию.", ""]
-(HERE/"backend-waves.md").write_text("\n".join(lines))
-print(f'Rendered {len(plan["waves"])} waves')
+    for title, key in [("Состав", "scope"), ("Предмет ревью", "review"), ("Проверка готовности", "acceptance")]:
+        lines += ["", '**'+title+':**', ""] + ['- '+text for text in wave[key]]
+    lines += ["", '**Подключение:** '+wave["activation"], ""]
+lines += ["## Поддержание графа", "", "Источник — [backend-waves.json](backend-waves.json), проверка — [wave_graph.py](wave_graph.py), генерация этой страницы — [render-waves.py](render-waves.py). Method/path/payload остаются в [build.py](../05-rest-api/build.py). После изменения пересобрать артефакты:", "", "```sh", "python3 docs/04-bitrix-modules/wave_graph.py docs/04-bitrix-modules/backend-waves.json", "python3 docs/04-bitrix-modules/render-waves.py", "python3 docs/05-rest-api/build.py", "python3 docs/05-rest-api/validate.py", "node docs/05-rest-api/validate-postman.cjs", "```", "", "Валидатор проверяет DAG независимо от порядка строк JSON, точные обратные unlocks, готовность начатых волн относительно принятого main, 35 legacy ID и 99 первичных API ID. Отрицательные fixtures отвергают циклы, неизвестные зависимости, открытые решения и старт поверх незамерженной волны. Ни один из этих результатов не означает live-проверку API.", ""]
+(HERE / "backend-waves.md").write_text("\n".join(lines))
+print(f'Rendered {state["waves"]} independent waves')
diff --git a/docs/04-bitrix-modules/wave_graph.py b/docs/04-bitrix-modules/wave_graph.py
new file mode 100644
--- /dev/null
+++ b/docs/04-bitrix-modules/wave_graph.py
@@ -0,0 +1,107 @@
+#!/usr/bin/env python3
+"""Validate wave dependencies and independent readiness; no network or Git writes."""
+from copy import deepcopy
+from pathlib import Path
+import json
+import re
+
+
+def validate_graph(plan):
+    assert plan["version"] == 2 and plan["planStatus"] == "active"
+    waves = plan["waves"]
+    by_id = {wave["id"]: wave for wave in waves}
+    assert len(by_id) == len(waves), "Duplicate wave ID"
+    assert all(re.fullmatch(r"[A-N][1-9]\d*", wid) for wid in by_id), "Invalid wave ID"
+    policy = plan["mergePolicy"]
+    assert policy["baseBranch"] == "main" and policy["dependenciesMustBeMerged"]
+    assert policy["verifyMainPlusOwnDiff"] and policy["decisionGatesMustBeResolved"]
+    assert policy["stackedPullRequests"] is False
+    merged = {wave["id"] for wave in waves if wave["deliveryState"] == "merged"}
+    assert merged == set(plan["baseline"]["mergedWaves"]), "Baseline/state mismatch"
+    accepted = set(plan["acceptedImplementationDecisions"])
+    assigned, legacy = {}, set()
+    for wave in waves:
+        wid = wave["id"]
+        deps = wave["dependsOn"]
+        assert len(deps) == len(set(deps)), ("Duplicate dependency", wid)
+        assert set(deps) <= by_id.keys() and wid not in deps, ("Unknown/self dependency", wid)
+        assert wave["unlocks"] == [other["id"] for other in waves if wid in other["dependsOn"]], ("Stale unlocks", wid)
+        assert wave["deliveryState"] in {"merged", "review", "inProgress", "planned"}
+        assert all(re.fullmatch(r"W\d{2}", item) for item in wave["legacyIds"])
+        legacy.update(wave["legacyIds"])
+        assert all(re.fullmatch(r"D\d{2}", item) for item in wave["decisionGates"])
+        for key in ["inputBoundary", "outputBoundary", "mergeCheck"]:
+            assert isinstance(wave[key], str) and wave[key].strip(), ("Missing boundary", wid, key)
+        for eid in wave["endpointIds"]:
+            assert eid not in assigned, ("Duplicate primary owner", eid)
+            assigned[eid] = wid
+        if wave["deliveryState"] in {"review", "inProgress"}:
+            assert set(deps) <= merged, ("Work depends on unmerged wave", wid)
+            assert set(wave["decisionGates"]) <= accepted, ("Work depends on open decision", wid)
+    assert legacy == {f"W{i:02}" for i in range(35)}, "Missing legacy mapping"
+    assert len(assigned) == plan["endpointCount"], "Primary endpoint coverage"
+    ancestors, visiting = {}, set()
+
+    def visit(wid):
+        assert wid not in visiting, ("Dependency cycle", wid)
+        if wid in ancestors:
+            return ancestors[wid]
+        visiting.add(wid)
+        result = set()
+        for dep in by_id[wid]["dependsOn"]:
+            result.add(dep)
+            result.update(visit(dep))
+        visiting.remove(wid)
+        ancestors[wid] = result
+        return result
+
+    for wid in by_id:
+        visit(wid)
+    for wave in waves:
+        for eid in wave.get("verifiesEndpointIds", []):
+            assert eid in assigned, ("Unknown verification endpoint", eid)
+            assert wave["id"] not in ancestors[assigned[eid]], ("Verification required before implementation", wave["id"], eid)
+    ready = [wave["id"] for wave in waves if wave["id"] not in merged
+             and set(wave["dependsOn"]) <= merged and set(wave["decisionGates"]) <= accepted]
+    return {"waves": len(waves), "endpoints": len(assigned), "legacyWaves": len(legacy), "readyFromMain": ready,
+            "blocked": [wave["id"] for wave in waves if wave["id"] not in merged and wave["id"] not in ready]}
+
+
+def negative_checks(plan):
+    scenarios = []
+    def rejects(label, mutate):
+        broken = deepcopy(plan)
+        mutate(broken)
+        try:
+            validate_graph(broken)
+        except (AssertionError, KeyError):
+            scenarios.append(label)
+        else:
+            raise AssertionError("Invalid fixture accepted: " + label)
+    def wave(data, wid):
+        return next(item for item in data["waves"] if item["id"] == wid)
+    def cycle(data):
+        wave(data, "C1")["dependsOn"].append("C2")
+        wave(data, "C1")["deliveryState"] = "planned"
+        for item in data["waves"]:
+            item["unlocks"] = [other["id"] for other in data["waves"] if item["id"] in other["dependsOn"]]
+    rejects("cycle", cycle)
+    rejects("unknown dependency", lambda data: wave(data, "E1")["dependsOn"].append("Z9"))
+    rejects("unmerged dependency started", lambda data: wave(data, "C2").update(deliveryState="inProgress"))
+    rejects("open decision started", lambda data: wave(data, "E1").update(decisionGates=["D02"]))
+    rejects("stale unlocks", lambda data: wave(data, "C1").update(unlocks=[]))
+    rejects("duplicate endpoint owner", lambda data: wave(data, "E1")["endpointIds"].append("ACC-01"))
+    rejects("missing legacy mapping", lambda data: wave(data, "B1").update(legacyIds=[]))
+    rejects("stacked PR permitted", lambda data: data["mergePolicy"].update(stackedPullRequests=True))
+    return scenarios
+
+
+if __name__ == "__main__":
+    import argparse
+    parser = argparse.ArgumentParser()
+    parser.add_argument("plan", type=Path)
+    args = parser.parse_args()
+    document = json.loads(args.plan.read_text())
+    result = validate_graph(document)
+    result["negativeFixtures"] = negative_checks(document)
+    print(json.dumps(result, ensure_ascii=False, indent=2))
diff --git a/docs/04-bitrix-modules/README.md b/docs/04-bitrix-modules/README.md
--- a/docs/04-bitrix-modules/README.md
+++ b/docs/04-bitrix-modules/README.md
@@ -13 +13 @@
-- `rebit.notification`: текущий Lead; общая надёжная доставка для MoreFoto проектируется в W20. REST чеков остаётся у Payments, файлов — у Files. LeadHunter сохраняет собственный CLI-процесс.
+- `rebit.notification`: текущий Lead; общая надёжная доставка для MoreFoto проектируется в H1. REST чеков остаётся у Payments, файлов — у Files. LeadHunter сохраняет собственный CLI-процесс.
@@ -106 +106 @@
-Подробная очередь: **[35 волн W00–W34](backend-waves.md)**. Каждая имеет ответственность, владельцев, зависимости, решения D, первичные REST-ID, повторные проверки и критерии подключения frontend. [JSON плана](backend-waves.json) связывает каждую из 99 операций ровно с одной первичной волной; [Postman по волнам](../05-rest-api/postman/MoreFoto.backend-waves.postman_collection.json) использует те же запросы, что основная коллекция.
+Активный план — **[39 независимых волн A1–N2](backend-waves.md)**. Буква обозначает направление, номер — самостоятельный срез; это не последовательная очередь. Каждая волна содержит `dependsOn`, обратные `unlocks`, вход/выход, primary REST ID и проверку отдельного merge. [JSON](backend-waves.json) — источник графа, [Postman по волнам](../05-rest-api/postman/MoreFoto.backend-waves.postman_collection.json) сохраняет те же 99 операций.
@@ -108 +108 @@
-Порядок: W00–W04 — существующая основа и её дефекты; W05 — результат R17 и domain/database; W06–W18 — доступ, подготовка и оформление; W19–W24 — деньги, общая доставка, исправления и решения; W25–W28 — файлы и поддержка; W29–W33 — печать, передача и полные сводки; W34 — сквозная приёмка.
+Каждый PR создаётся от актуального `main` и направлен в `main`. Все его runtime/compile/schema зависимости уже должны находиться в main; draft и незамерженная ветка не удовлетворяют зависимости. Готовые волны выполняются одновременно, заблокированные пропускаются. Stacked PR, перенос чужого незамерженного кода, allow-all и фиктивный fallback запрещены. Независимость проверяется на main плюс только собственный diff. При изменении base повторяются затронутые проверки.
@@ -110 +110 @@
-Одна волна обычно равна одному PR с работающим сценарием, миграциями и значимыми проверками. 3–4 тысячи строк — ориентир для удобства ревью, не лимит. Большую волну делить по ответственности; не разделять предметный сценарий на отдельные PR всех DTO, репозиториев и тестов.
+A1–A6 (W00–W05) слиты; B1 (W06) на ревью в PR #8. Сейчас независимы **C1 — внутренние учреждения** и **E1 — внутренний каталог**; у них нет HTTP и зависимости от B1. Защищённые C2/E2 начинаются после merge B1 и своего внутреннего среза. C3 добавляет реальные съёмки/группы, затем C4 — полный ORG-04. Минимальные назначения Access входят в C2/C3; B2 добавляет управление сотрудниками. A7 фиксирует правило и не является runtime-блокером.
@@ -112 +112 @@
-Реализация endpoint не равна готовности всех полей его ответа: назначения повторно проверяются в W09, платежи/возвраты — W19–W24, полные карточки и сводки — W33. Нельзя скрывать недостающий поставщик данных нулём или frontend-моком. Новые продуктовые сценарии начинаются после бизнес-проверки R17, открытые решения не считаются принятыми.
+Утверждённые в A6 рекомендации D07/D08/D10 разрешают реализацию; бизнес-проверка R17 остаётся отдельной приёмкой. D02 не блокирует пустое редактируемое хранилище Product, но его ассортимент/цены не считаются утверждёнными и коммерческие условия E3 остаются зависимыми от решений. Открытые решения остальных направлений сохраняются.
@@ -114,3 +114 @@
-При детальной сверке добавлены ранние задачи: маскирование логов W01; ACTIVE/pending/TTL W02; несовместимый вызов Symfony Validator в multipart и устойчивый владелец файла W03; optional secret stdout и PATCH/CORS W04. Эти дефекты пока не исправлены. Подробности и критерии — в [плане](backend-waves.md#находки-аудита-и-волны-исправлений).
-
-Команды генерации и офлайн-проверки — в [карте API](../05-rest-api/README.md#поддержание-артефактов). Они не выполняют HTTP и не подтверждают доставку Telegram/email. Подготовка плана не реализует новые бизнес-API.
+Все прежние W00–W34 имеют [явную карту соответствия](backend-waves.md#соответствие-прежним-wnn). Замороженный снимок W00 не переписывается. API не становится реализованным от изменения номера; повторная проверка не является обратной runtime-зависимостью. Готовность полного ответа и целого frontend-сценария проверяется отдельно.
diff --git a/docs/05-rest-api/build.py b/docs/05-rest-api/build.py
--- a/docs/05-rest-api/build.py
+++ b/docs/05-rest-api/build.py
@@ -88 +88 @@
-    note="Принятое D08: адресное приглашение и самостоятельная установка пароля через Auth; пригодность штатного механизма проверяется в W09. При назначениях обязательна актуальная assignmentSignature; пароль не возвращается этим API.")
+    note="Принятое D08: адресное приглашение и самостоятельная установка пароля через Auth; пригодность штатного механизма проверяется в B2. При назначениях обязательна актуальная assignmentSignature; пароль не возвращается этим API.")
@@ -113 +113 @@
-    note="Принята W05: shoots/groups — независимые {items,meta{page,pageSize,total}}, сортировка createdAt desc + id. Summary обязательный: unavailable{reason=dependenciesNotReady} до финансового поставщика либо ready{currency=RUB,paidOrderCount,paidAmount,confirmedRefundAmount,pendingRefundAmount,netAmount,calculatedAt}; суммы целые копейки, net=paid-confirmedRefund. Нули только при реальной пустой выборке. До W08/структурного поставщика полный endpoint даёт 503; после W08 допустима карточка с unavailable. Сбой подключённого поставщика — 503. AssignmentSignature только в проекции организатора; teacher не имеет ORG-04.")
+    note="Принята A6 (W05): shoots/groups — независимые {items,meta{page,pageSize,total}}, сортировка createdAt desc + id. Summary обязательный: unavailable{reason=dependenciesNotReady} до финансового поставщика либо ready{currency=RUB,paidOrderCount,paidAmount,confirmedRefundAmount,pendingRefundAmount,netAmount,calculatedAt}; суммы целые копейки, net=paid-confirmedRefund. Нули только при реальной пустой выборке. До C3/структурного поставщика полный endpoint не подключается; C4 допускает карточку с unavailable. Сбой подключённого поставщика — 503. AssignmentSignature только в проекции организатора; teacher не имеет ORG-04.")
@@ -438 +438,4 @@
-    assert plan["version"] == 1 and plan["planStatus"] == "proposed"
+    import sys
+    sys.path.insert(0, str(plan_path.parent))
+    from wave_graph import validate_graph
+    validate_graph(plan)
@@ -444,2 +447 @@
-        assert re.fullmatch(r"W\d{2}[A-Z]?", wid) and wid not in seen, ("Wave ID",wid)
-        assert set(wave["dependsOn"]) <= seen, ("Wave dependencies must precede it",wid)
+        assert re.fullmatch(r"[A-N][1-9]\d*", wid) and wid not in seen, ("Wave ID",wid)
@@ -663 +665 @@
-- [План последовательных волн](../04-bitrix-modules/backend-waves.md) и [Postman по волнам](postman/MoreFoto.backend-waves.postman_collection.json) — те же операции, папки для отдельного ревью. Пустые волны содержат работу без новых HTTP; это не сквозной Run all.
+- [Граф независимых волн](../04-bitrix-modules/backend-waves.md) и [Postman по волнам](postman/MoreFoto.backend-waves.postman_collection.json) — те же операции, папки для отдельного ревью. Пустые волны содержат работу без новых HTTP; это не сквозной Run all.
@@ -671 +673 @@
-[Карта модулей и результаты аудита](../04-bitrix-modules/README.md) фиксируют владельцев сценариев и правила реализации. [Предметная область](../02-domain/README.md) и [БД](../03-database/README.md) ещё требуют бизнес-проверки: готовой схемы highload-блоков и утверждённых имён новых модулей нет. Здесь предложены владельцы ресурсов и контракты без выдуманных таблиц, полей UF_* и заявлений о готовом сервере. Привязка к хранению — следующий этап.
+[Карта модулей и результаты аудита](../04-bitrix-modules/README.md) фиксируют владельцев сценариев и правила реализации. [Предметная область](../02-domain/README.md) и [БД](../03-database/README.md) содержат принятую в A6 (W05) модель и схему Access/Organization. Остальные части уточняются перед соответствующей волной; бизнес-проверка R17 и открытые коммерческие решения сохраняют свой статус. Наличие записи в реестре не доказывает готовый HTTP.
@@ -1019,0 +1022 @@
+            ". Разблокирует при выполнении остальных условий: "+(", ".join(wave["unlocks"]) or "нет")+
diff --git a/docs/05-rest-api/validate.py b/docs/05-rest-api/validate.py
--- a/docs/05-rest-api/validate.py
+++ b/docs/05-rest-api/validate.py
@@ -34 +34,5 @@
-    assert wave_plan["version"]==1 and wave_plan["planStatus"]=="proposed" and wave_plan["endpointCount"]==len(records)
+    import sys
+    sys.path.insert(0, str(ROOT.parent/"04-bitrix-modules"))
+    from wave_graph import validate_graph
+    graph_result=validate_graph(wave_plan)
+    assert wave_plan["endpointCount"]==len(records)
@@ -40 +44 @@
-        assert re.fullmatch(r"W\d{2}[A-Z]?",wid) and wid not in seen,("Duplicate/invalid wave",wid)
+        assert re.fullmatch(r"[A-N][1-9]\d*",wid) and wid not in seen,("Duplicate/invalid wave",wid)
@@ -42 +45,0 @@
-        assert len(set(wave["dependsOn"]))==len(wave["dependsOn"]) and set(wave["dependsOn"])<=set(seen),("Dependency order",wid)
@@ -58 +60,0 @@
-        assert all(order[wid]>=order[ep["implementationWave"]] for wid in expected_verification),("Regression before implementation",ep["id"])
@@ -181 +183 @@
-    files=["build.py","validate.py","validate-postman.cjs","../04-bitrix-modules/README.md","../04-bitrix-modules/backend-waves.json","../04-bitrix-modules/backend-waves.md","../04-bitrix-modules/render-waves.py",
+    files=["build.py","validate.py","validate-postman.cjs","../04-bitrix-modules/README.md","../04-bitrix-modules/backend-waves.json","../04-bitrix-modules/backend-waves.md","../04-bitrix-modules/render-waves.py","../04-bitrix-modules/wave_graph.py",
@@ -191 +193 @@
-        "wave primary coverage and ordered dependencies","wave metadata and review/acceptance completeness",
+        "wave primary coverage, acyclic dependencies and independent readiness","wave metadata and review/acceptance completeness",
