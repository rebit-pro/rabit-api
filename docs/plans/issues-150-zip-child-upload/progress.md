# #150: прогресс

## Точка продолжения

- Ветка: `codex/issues-150-zip-child-upload`, base main `591d698` (слит 26.09.2026, J1 #143); последний код — `c53a149`.
- Issue: https://github.com/rebit-pro/rabit-api/issues/150. PR: https://github.com/rebit-pro/rabit-api/pull/151.
- Документация: `docs/plans/issues-150-zip-child-upload/plan.md`.
- Готово: backend (шаг 4), frontend (шаг 5), быстрые проверки и self-review (шаг 6), live E2E-спек и визуальная
  проверка desktop/mobile на частичном прогоне группы `a` (шаг 7).
- Сейчас: PR готов к ревью. Следующий шаг: ревью пользователя; после ревью без блокеров — полный `make test-e2e`
  (шаг 8), затем реальная загрузка съёмки 158 на stage (T15, T19) с проверкой места на диске (шаг 3a).
- Блокеры: нет. Для съёмки 158 групповые папки `F`, `L`, `U`, `AX` отмечаются переключателем на сверке (или
  скрипт подготовки переименовывает их в `GROUP-…`).
- Рабочее дерево: всё закоммичено и отправлено.
- Следующая проверка: `make test-e2e E2E_PHP_CLI_IMAGE=rabit-api-php-cli:d1-local E2E_PHP_FPM_IMAGE=rabit-api-php-fpm:d1-local
  E2E_KERNEL_ROOT=/home/user/rebit-p2p/api/public/bitrix E2E_VENDOR_ROOT=/home/user/rabit-api/api/vendor`.

## Тест-кейсы

| ID | Статус | Дата | Команда | Доказательство |
|---|---|---|---|---|
| T01 | PASS | 2026-09-26 | phpunit `--filter UploadChildAssignmentTest` | `testCodesPattern` 12 случаев, `testMapperSplitsCodesAndDropsRepeats` |
| T02 | PASS | 2026-09-26 | то же | `testGroupPhotoIsAssignedToEveryCodeUnderOneRevision` |
| T03 | PASS | 2026-09-26 | то же | `testRepeatedUploadWithKnownCodesKeepsTheRevision`, `testDuplicateUploadLabelsThePhotoThatOwnsTheContent` |
| T04 | PASS | 2026-09-26 | то же | `testPhotoOutsideTheUploadGroupIsNotLabeled` |
| T05 | PASS | 2026-09-26 | то же + `PhotoWorkflowTest` | `testGroupHandedOverWhileWaitingForTheLockRejectsTheLabels`, прежний `testUploadRejectsPublishedGroup…` |
| T06 | PASS | 2026-09-26 | `node --test tests/photos/archive.test.mjs` | обёртка, `__MACOSX`, служебные, вложенные, вне папок, пустые, > 25 МБ |
| T07 | PASS | 2026-09-26 | то же | коды, `GROUP…`, ручная отметка, кириллица с подсказкой, `ABCD` |
| T08 | PASS | 2026-09-26 | `node --test tests/photos/zip-reader.test.mjs` | CP866 без флага и UTF-8 дают одинаковые имена |
| T11 | PASS | 2026-09-26 | `tests/photos/upload-retry.test.mjs` | `sessionTooShort` для 4,4 ГБ при 1 МБ/с |
| T12 | PASS | 2026-09-26 | `tests/photos/archive.test.mjs` | > 2000 файлов, групповые без детей, архив без фото |
| T17 | PASS (структура) | 2026-09-26 | `tests/photos/zip-reader.test.mjs` | каталог Zip64 через локатор; архива > 4 ГБ под рукой нет — браузерная проверка памяти остаётся PENDING |
| T18 | PASS (unit) | 2026-09-26 | `tests/photos/archive.test.mjs` | ребёнок из двух частей — один, тома отклоняются; live-часть — PENDING |
| T20 | PASS | 2026-09-26 | `tests/photos/upload-retry.test.mjs` | классификация и паузы 2→120 с |
| T22 | PASS (unit) | 2026-09-26 | `tests/photos/upload-retry.test.mjs` | прогресс переживает перезагрузку, сломанное хранилище не ломает загрузку; live-часть — PENDING |
| T09 | PASS | 2026-09-26 | `E2E_GROUPS=a make test-e2e …` (run `rabit-e2e-e77a9486a14b`) | `zz-media-archive` #1: два архива, сверка, 5 из 5, номера `A001…A004`, `B001…B003`, без ребёнка 0 |
| T10 | PASS | 2026-09-26 | то же | повтор: своих кадров не отправлено, групповые дали `duplicate`, готовых кадров 6 |
| T13 | PASS | 2026-09-26 | то же | прежние `zz-media` (D1/D2, #33, #54/#55, #106, #115/#116) зелёные |
| T14 | PASS | 2026-09-26 | то же | снимки 1440 и 390 px, `scrollWidth <= clientWidth`; заголовки таблицы на 390 px исправлены (`c53a149`) |
| T16 | PASS | 2026-09-26 | то же | ребёнок `C` через API `childCodes=C`, повторный выбор архивов → `C002`, `C003` групповые |
| T18 | PASS | 2026-09-26 | то же + unit | две части одним выбором, групповые получили всех детей |
| T21 | PASS (503) | 2026-09-26 | то же | две отправки с 503 повторены сами; офлайн-пауза покрыта unit T20, живой `setOffline` не проверялся |
| T22 | PASS | 2026-09-26 | то же | новая вкладка: «Отправлено ранее и пропущено: 3», отправлены только 2 групповых |
| T15, T19 | PENDING | 2026-09-26 | — | реальная съёмка 158 и файл 25 МБ на stage — после ревью |

## Журнал

### 2026-09-26

- Изучен текущий поток: `UploadPhotoUseCase` (приём без кода), `AssignPhotosUseCase` (только `ready`, revision,
  идемпотентность, ≤100 кадров), `usePhotoQueue` (2 потока, 2000 файлов, `sessionStorage`), коды `^[A-Z]{1,3}$`.
- Ограничения сервера: PHP `post_max_size = 200M` → серверный приём архива съёмки невозможен без смены лимитов;
  выбрана распаковка в браузере (ZIP-DEC-01).
- Открытых issue/PR по ZIP-загрузке нет; создан #150.
- Подготовлен план; решения ZIP-DEC-02/03 вынесены пользователю, остальные — с рекомендацией.
- Ответ пользователя: папки только латиницей (кириллица была от диктовки) → ZIP-DEC-02 «только латиница».
  `AA`/`BB` — групповые фото, которые кладут один раз вместо копирования в папку каждого ребёнка → ZIP-DEC-03:
  удвоенная буква = групповой набор, кадр привязывается ко всем детям группы (архив + уже размеченные).
- Поле загрузки стало `childCodes` (строка через запятую): multipart-поля `rebit.share` (`FormField`) только
  строковые. Добавлен T16 (догон групповых кадров для нового ребёнка повторной загрузкой).
- Пользователь: съёмка 4,4 ГБ, делится на архивы по ~500 МБ; требование — большие объёмы без отказов и отката.
  Найдено: 401 сбрасывает сессию и уводит на вход (`frontend/src/api/http.ts`), сессия 24 ч без продления
  (`REBIT_TOKEN_TTL_HOURS`); сетевые ошибки не повторяются автоматически; черновик очереди в `sessionStorage`.
  Добавлены ZIP-DEC-07 (несколько архивов одним планом, Zip64, тома не поддерживаются) и ZIP-DEC-08 (сверка
  лимитов до старта, автоповтор, пауза офлайн/401, прогресс в `localStorage`, Wake Lock, проверки stage),
  тест-кейсы T17–T22, шаг 3a.
- Шаг 3 — проверка реальной съёмки 158 (оригиналы и `158_parts` в Downloads пользователя; имена детей и
  персональные данные в архивах отсутствуют, в журнал не переносятся):
  - оригиналы: 591 JPEG, 4,73 ГБ, все 3000×4500 (13,5 Мп), размер 6,3–10,1 МБ → лимиты 25 МБ / 40 Мп
    не нарушаются, отказов по лимитам не будет;
  - части: 11 самостоятельных ZIP (`158_part01…11.zip`, 71 МБ–492 МБ), метод store, без Zip64, флаг UTF-8
    не выставлен (имена ASCII, декодирование не важно); структура `<КОД>/<КОД><NNN>.jpg`, без общего корня,
    глубина 2; каждая папка целиком в одной части; 591 файл, повторов имён нет;
  - 63 папки подряд: `A…Z`, `AA…AZ`, `BA…BK`; рядом `158_соответствие.csv` (Архив;Буква;Тип;Новое имя;
    Исходный файл): 59 папок — «ребёнок» (в т. ч. `AA`, `BB`), 4 папки — «группа»: `F` (9), `L` (1), `U` (2),
    `AX` (3), всего 15 групповых кадров;
  - вывод: правило ZIP-DEC-03 «удвоенная буква = групповые» на реальных данных неверно — при 59 детях `AA`, `BB`
    обычные дети. Групповую папку по имени отличить нельзя; нужен явный признак.
- Ответ пользователя: групповые папки отмечаются префиксом `GROUP` (`GROUP-1`, `GROUP-F`), на сверке есть
  ручной переключатель; групповой кадр получают все дети группы. ZIP-DEC-03 переписано, T07/T09/T15/T18 обновлены.
- Backend: необязательное multipart-поле `childCodes` (`^[A-Z]{1,3}(,[A-Z]{1,3}){0,99}$`, 422 `INVALID_CHILD_CODES`),
  Application Service `UploadChildAssignment` (транзакция, `lockRevision`, повторная проверка редактируемости →
  409 `GROUP_MEDIA_LOCKED`, `groupPhoto` без условия `ready`, `child` + `assign` на каждый код, одно продвижение
  revision). Дубль размечает кадр-владелец содержимого (`existingPhotoId`), кадр другой группы не размечается.
  Разметка идёт после постановки превью в очередь, чтобы сбой разметки не задерживал превью. `LogSanitizer`
  пропускает `assignMs`; число кодов пишется в существующий `added`.
- Проверки (образ `rabit-api-php-cli:d1-local`, vendor основного checkout, `--network none`):
  `phpunit --testsuite=unit` → OK 855 тестов; `php-cs-fixer --dry-run` по 15 файлам → 0 правок;
  `phpstan analyse` по 15 файлам → No errors.
- Frontend: `zip-reader.ts` (свой читатель вместо `@zip.js/zip.js`, план ZIP-DEC-01 обновлён), `archive.ts` (план
  загрузки), `upload-retry.ts`, `archive-progress.ts` (`localStorage`), `useArchivePlan.ts`, `ArchivePlan.vue`,
  `ArchiveProgress.vue`; `usePhotoQueue`: `addArchive`, ленивое извлечение записи, автоповтор/офлайн/401/409,
  пропуск принятых ранее, Wake Lock; `PhotoUpload.vue`: переключатель «По фото / ZIP по детям»; иконка
  `mdi-folder-zip-outline` в реестре.
- Реальная съёмка 158 через читатель и планировщик (node 22, `openAsBlob`, архивы и оригиналы read-only):
  оглавление 11 частей за 55 мс; план 591 фото / 4,73 ГБ / 59 детей / групповые `F:9, L:1, U:2, AX:3` при ручной
  отметке, получателей 59, проблем 0, пропусков 0; первая запись каждой части побайтно совпала с оригиналом
  (SHA-256) по `158_соответствие.csv` — 11 из 11.
- Проверки frontend (образ `mcr.microsoft.com/playwright:v1.52.0-jammy`, volume `rabit-issues92-node`, `--network none`):
  `npm run check` → exit 0, `test:ui` 81 pass / 0 fail (в т. ч. 14 новых в `tests/photos`); `npm run build-only` →
  собрано, `PhotoWorkspacePage` 86,18 КБ (gzip 29,43 КБ).
- Уточнения очереди после проектирования E2E: групповой файл ждёт, пока все файлы детей той же группы будут приняты
  (иначе при двух потоках групповой кадр мог получить номер раньше последнего своего кадра ребёнка); пропуск
  «отправлено ранее» применяется только к файлам детей — групповые при повторном выборе отправляются снова, чтобы
  догнать привязки новых детей (T16), сервер второй кадр не создаёт.
- E2E: новый `frontend/e2e/live/zz-media-archive.spec.ts` (группа `a` в `groups.json`), `png`/`crc32`/`pngVariant`
  перенесены из `zz-media.spec.ts` в `helpers.ts`. Сценарии: два архива, ручная отметка `F`, пропуск `.txt`,
  «Отправлено/Превью готовы 5 из 5», номера своих и групповых кадров, догон ребёнка `C` повторным выбором (T09, T10,
  T16, T22); кириллическая папка блокирует старт, 503 на две первые отправки повторяются сами, экраны 390 px (T14,
  T21). `npm run check` → exit 0, 81 pass.
- E2E, частичный прогон группы `a` (не полный gate): первый прогон нашёл дефект — ключ архива включал
  `lastModified`, повторно выбранный архив считался новым и дети отправлялись заново (10 вместо 5); ключ теперь
  имя + размер. Второй — неверное ожидание теста (строки `duplicate` входят в общий счёт). После правок:
  `rabit-e2e-59e86018d566` и финальный на `c53a149` — `rabit-e2e-e77a9486a14b`: 81 passed, exit 0 (~7 мин).
- Визуальная проверка: номера шли не по именам файлов (`A001.png` → `A002`) при двух потоках на одного ребёнка →
  очередь отправляет кадры одного ребёнка по одному, разных детей — параллельно (`inOrder`); в режиме ZIP скрыта
  пустая кнопка «Загрузить на сервер»; на 390 px заголовки таблицы сверки переносились по буквам → `nowrap`.
