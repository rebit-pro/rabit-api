# OPS — база знаний /guide/ — журнал

## Точка продолжения

- Ветка `codex/ops-guide-knowledge-base`, base `main` `4ca7e9c`. Worktree `/home/user/rabit-api-worktrees/ops-guide-knowledge-base`.
- Стенд для скриншотов (остановлен `make e2e-down`): detached worktree `/home/user/rabit-api-worktrees/guide-shots-stand`
  на `7ec736a`. Скрипт съёмки `shots.mjs` и исходные PNG — в scratchpad сессии (`guide-shots/v2/`).
- Документация: [план](plan.md), предыдущая итерация [OPS-stage-run-guide](../OPS-stage-run-guide/progress.md).
- Завершено: главная с плитками, 7 статей, 44 скриншота WebP (1,8 МБ), проверки KB-T01…T05.
- [PR #95](https://github.com/rebit-pro/rabit-api/pull/95) слит пользователем разрешённым merge как `d4fe5a1`; выкачен
  frontend-only релиз `guide-kb-20260926071636-e69ee57-d4fe5a1` (живой frontend `e69ee57` + `guide/` из `d4fe5a1`).
- Следующий шаг: задача закрыта; неблокирующее — #109, #110.
- Блокеров нет.
- Рабочее дерево: новые/изменённые файлы `frontend/public/guide/**`, `docs/plans/OPS-guide-knowledge-base/**`.

## Тест-кейсы

| ID | Статус | Дата | Команда / доказательство |
| --- | --- | --- | --- |
| KB-T01 | PASS | 2026-09-25 | `npm run check` (том `rabit-issues42-node`), код выхода 0; до исправления `guide.js` ESLint давал 38 ошибок |
| KB-T02 | PASS | 2026-09-25 | `git archive 7ec736a frontend` + замена `public/guide` на содержимое ветки; `docker build -f frontend/docker/production/nginx/Dockerfile` |
| KB-T03 | PASS | 2026-09-25 | Playwright-обход 8 страниц: 68 внутренних ссылок и якорей без ошибок, все 44 изображения загружены, `static/` 200 |
| KB-T04 | PASS | 2026-09-25 | 1280×900 light, 390×844 light/dark: overflow 0 (после переноса длинных `.ui`), ошибок и внешних запросов 0 |
| KB-T05 | PASS | 2026-09-25 | Чек-лист: отметки и поля восстановлены после reload, «Открыть» активна; данные первой версии (`…-v1`) подхватываются |
| KB-T06 | PASS | 2026-09-26 | `curl https://app.morefoto36.ru`: 8 страниц `/guide/**` 200 со своими `<title>`, 10 плиток, `static` и изображения 200; `/login`, `/cabinet/payments` 200 (SPA), `/health` 200, `/api/v1/me` 401; `index-DBtbNwZZ.js` совпадает с прежним |
| KB-T01 (повтор) | PASS | 2026-09-26 | `npm run check` на ветке после merge `main` 03f4e3b, код выхода 0 |
| KB-T03/T04/T05 (повтор) | PASS | 2026-09-26 | Образ `d8caca4` + guide: 8 страниц, 75 ссылок и якорей, 49 изображений; overflow 0 на 1280/390 light/dark; внешних запросов и ошибок 0; чек-лист восстанавливается (16 отметок) |
| KB-T07 | PASS | 2026-09-26 | Шаг «Оплатить картой» со скриншотами p07–p10 (реальная оплата тестовой картой на стенде `d8caca4`), «Платежи» c07/c08 |

## Журнал

### 2026-09-25

- Запрос пользователя: инструкция непонятна Алёне и Рите; нужны шаги по ролям со скриншотами, как получить ссылки,
  больше статей плитками, повторная выкладка.
- `main` на `4ca7e9c` содержит #78 и #84 с изменениями backend, которые не выложены; выложен frontend `7ec736a`
  (backend `54bd4ab`). Решение OPS-KB-DEC-02/03: скриншоты и образ — от `7ec736a`, из ветки берётся только `guide/`.
- `make e2e-up` на `7ec736a`: все стадии passed за 49 с (lint, PHPStan, PHPUnit, frontend checks, build, fixture).
- Права сверены по коду: передача ссылки — организатор, куратор, ответственный группы (`LinkPermissionPolicy`);
  списки сотрудников создают организатор и ответственный, руководитель только читает (`StaffRequestWorkflow`).
- Скриншоты снял агент на стенде (61 снимок, отобрано 44, WebP: desktop 1280 px q78, телефон 600 px q80). Найденные
  расхождения с первой версией инструкции: куратора, ответственного и руководителя назначают в карточке сотрудника
  («Назначенные учреждения/группы»), а не в формах учреждения и группы; «Ссылки и сроки» — только в меню; телефон
  при оформлении обязателен; выбор канала чека недоступен до оплаты; код ребёнка — 1–3 латинские буквы.
- Проверено по коду: до «Отметить передачу» галерея в состоянии `preparing` и показывает «Ответственный сообщит…»
  (`GalleryAvailability`, `GalleryScreen.vue`).
- Особенности стенда при съёмке: организатор фикстуры переименован в «Ольга Морозова», куратор — в «Рита Иванова»
  (`rita@example.invalid`); обработчик фото стенда останавливается через 15 минут и дважды запускался вручную;
  на демо-фото два водяных знака (свой и серверный).
- Статьи: путь заказа, организатор (9 шагов), куратор (5), родитель (5), ответственный и руководитель, частые вопросы,
  тестовый прогон (14 отметок, поля в localStorage).
- Релизный образ `morefoto-frontend:guide-kb-20260925151854-7ec736a-b314d77` собран из `7ec736a` + `frontend/public/guide` из `b314d77`; smoke
  контейнера: `/guide/**`, изображения, `/login`, `/health` — 200. Пакет с `SHA256SUMS` и `switch-frontend.sh` не загружен.
- По поручению пользователя включено оформление заказов на stage: `docker service update --env-add
  MOREFOTO_CHECKOUT_ENABLED=1 morefoto_stage_fpm` — converged, 1/1 на `rabit-api-php-fpm:d3-webp`; `getenv` в FPM
  возвращает `"1"`; `/api/v1/me` 401, `/login` и `/guide/` 200, неизвестная галерея — JSON `GALLERY_NOT_FOUND`.
  Откат: `docker service update --env-rm MOREFOTO_CHECKOUT_ENABLED morefoto_stage_fpm`.

### 2026-09-26

- Пользователь вернулся после перезагрузки. PR #95 открыт; `main` `03f4e3b`. На сайте — релиз `d8caca4` (G1 + #78/#84/#85/#86/#96),
  #100 слит, но не выложен (DTO serialized name, backend). SSH до `rebit-pro` временно недоступен (таймаут).
- Устаревает в статьях: «оплата не подключена» (путь, родитель, FAQ, прогон), скриншоты оформления и заказа родителя,
  список сотрудников (`UiDataTable` из #96).
- Стенд `make e2e-up` на `d8caca4` с тестовым магазином (`E2E_YOOKASSA_ENV`), все стадии passed за 55,6 с. Агент переснял все
  44 снимка и 5 новых (оплата p08–p10, «Платежи» c07/c08); браузер в сети `container:<frontend>` с базой `http://127.0.0.1`
  (адрес возврата ЮKassa). На снимках родителя `sbp` вырезан из `payment-quote` через `page.route`, так как на сайте
  `MOREFOTO_PAYMENT_METHODS=bank_card`; в o05 фикстурные учётки скрыты тем же способом. Заказ MF-000001 оплачен
  по-настоящему в тестовом магазине. Стенд остановлен.
- Изменения текстов: тип группы «Обычная группа»; строка загрузки «В работе: 0»; у родителя шаг 6 «Оплатить картой»
  (тестовая карта, «Заказ оплачен», «Оплачено»); у куратора шаг 6 «Проверить оплату»; путь, FAQ и прогон — про
  тестовую оплату вместо «не подключена».
- Найдено вне scope (оформить issues): плитка «Заказы» «Оплата появится после подключения платёжного провайдера»;
  при работающей оплате «Состав и демонстрационные условия проверены» и «Создать тестовый заказ»; «3 записей» в таблице
  сотрудников; телефон покупателя без форматирования в заказе и «Заказах».

### 2026-09-26 (вечер по UTC 25.09)

- Ревью PR #95 агентом: блокеров нет; issues #109 (тексты оформления и «Заказов» после G1), #110 (формат телефона).
  «3 записей» уже исправлено в `main` (#103).
- Merge агентом отклонён автоматическим режимом («Self-Approval»).
- Пакет `guide-kb-20260925200604-d8caca4-4655ec2` собран, но устарел до выкладки: за это время выкачены K3
  (backend `caa37b6`, сервисы support) и #103 (frontend `e69ee57`, содержит K3). Сравнение бандлов без хэшей показало
  в живом `QuestionsPage`/`QuestionThread`. Правило: образ собирать от живого frontend в момент выкладки.
- Скриншоты сняты на `d8caca4`: в меню кабинета на сайте могут быть пункты K3, которых нет на снимках; шаги инструкций
  от этого не меняются.

### 2026-09-26

- Пользователь разрешил merge и выкладку; PR #95 слит (`d4fe5a1`). В `main` также #102 (доработки оплаты, frontend
  `usePaymentReturn.ts`), не выложен — в релиз не входит.
- Живой frontend `morefoto-frontend:issues103-20260925200321-e69ee57`, backend `k3-20260925204828-caa37b6`. Образ собран
  из `git archive e69ee57 frontend` + `frontend/public/guide` из `d4fe5a1`; бандл приложения `index-DBtbNwZZ.js` совпал
  с живым (тот же хэш и размер) — отличается только `/guide/`.
- Релиз `/srv/morefoto/releases/guide-kb-20260926071636-e69ee57-d4fe5a1` (sha256 `b1250425…c26a`), `switch-frontend.sh`:
  converged, 2/2. Откат: `docker service rollback morefoto_frontend` (прежний образ в `frontend-before.txt` —
  `issues103-20260925200321-e69ee57`). Backend не менялся. KB-T06 PASS.
