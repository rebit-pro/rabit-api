# Дизайн и UX кабинета MoreFoto — журнал

## Точка продолжения

- Дата: 2026-09-23.
- Ветка: `codex/design-ux-plan` от `main` `5b750c07e964e279e3517292dae6517643f3e7be`; к моменту merge `origin/main` = `93becd8` (D3), пересечений с документами плана нет.
- PR: [#50](https://github.com/rebit-pro/rabit-api/pull/50) — сливается по указанию пользователя от 2026-09-23 после коммита с решениями и порядком доставки.
- Документация: [план](plan.md); соседний дизайн-план v1 `../MoreFoto/docs/frontend/design-plan.md`, руководство компонентов `ui-components.md`, иконка `icon-v1.md`, реестр решений `business-review/decisions.md` (D01, D08).
- Завершено: план, проверки DP-01…DP-05, решения DS-01…DS-20 (все рекомендации), порядок доставки единым PR.
- Следующий шаг: реализация в ветке `codex/u-design-system` от актуального `main`; журнал — `docs/plans/U_design-system/progress.md`.
- Блокеры: нет. Незакрытый риск: независимая сверка фактов плана с кодом агентом не завершена — выполняется по ходу волн (каждая волна сверяет свои факты перед изменением кода).
- Рабочее дерево: чистое после коммита.
- Команда следующей проверки: `python3 tools/verify-wave-graph.py docs/waves/graph.json`.

## Тест-кейсы PR

| ID | Статус | Дата | Команда | Доказательство |
| --- | --- | --- | --- | --- |
| DP-01 | PASS | 2026-09-22 | `git status --short; git diff --stat main --` | только `?? docs/plans/design-ux-plan/` |
| DP-02 | PASS | 2026-09-22 | скрипт: пути в обратных кавычках с расширениями php/vue/ts/scss/md/json/py/mjs/html/svg → `os.path.exists` по корню, `frontend/src`, модулям и `find` по имени | 106 кандидатов; 25 «не найдено» — будущие файлы волн (`tokens.ts`, `_tokens.scss`, `MfLogo.vue`, `statusTone.ts`, `chartPalette.ts`, `avatar.spec.ts`, `tokens-build.mjs`, `auth/errors.ts`) и намеренно отсутствующий `auth-pattern-dark.svg`; документы соседнего проекта проверены отдельно `test -f` — все 7 существуют |
| DP-03 | PASS | 2026-09-22 | `grep -nE "password=\|token=\|@(gmail\|mail\|yandex)\." docs/plans/design-ux-plan/*.md \| grep -v DP-03` | пусто |
| DP-04 | PASS | 2026-09-22 | `python3 tools/verify-wave-graph.py docs/waves/graph.json` | `waves: 40, endpoints: 99`, все негативные fixtures проходят; граф не изменён |
| DP-05 | PASS | 2026-09-22 | скрипт подсчёта `\|` в строках таблиц | после исправления строки 408 (символ `\|` внутри кода) расхождений 0 |

## Хронология

### 2026-09-22 — бриф и разведка

- Бриф пользователя: усилить дизайн кабинета app.morefoto36.ru — больше инфографики, аватарки сотрудников с буквами вместо фото, современный «дорогой» вид, пастельные цвета плюс синий цвет моря, дорогой логотип кабинета, проработка UX (пример: сотруднику выдают логин, а пароль не выдают), токен-система. Сейчас только план отдельным PR; реализация позже.
- Дополнение пользователя: тени использовать как выразительный инструмент; чаты с клиентами (сообщения будут приходить с сайта) — будущее направление, сейчас не в scope.
- Факты из кода, влияющие на план:
  - Тема `frontend/src/theme/MoreFotoTheme.ts` только светлая, цветовых токенов нет; в live-компонентах 189 hex в 47 файлах (`#5e6872` ×42, `#24658a` ×27, `#dce4ea` ×18).
  - Шрифт в кабинете Arial (`frontend/src/styles/morefoto.scss:4`), web-шрифты не подключены; предложение Ubuntu/Arsenal из design-plan.md не реализовано.
  - Бренд в шапке — текст «Море фото» (`.mf-brand`); знак существует только как иконка v1 (`frontend/public/icons/morefoto-v1.svg`).
  - Аватарок нет ни в `ProfileOutputDto`, ни в `StaffOutputDto`, ни в UI.
  - Онбординг: `POST /api/v1/users` создаёт Bitrix-пользователя со случайным паролем и `UF_AUTH_REGISTRATION_PENDING=1` (`rebit.auth/lib/Infrastructure/Adapter/StaffIdentityGateway.php:28`), письмо не отправляется; маршрут `/register` перенаправляет на `/login` (`frontend/src/router/PublicRoutes.ts:59`), поэтому у сотрудника нет пути задать пароль. Сброса/смены пароля в `rebit.auth` нет.
  - Финансовые сводки существуют только в demo; в live принят «unavailable» до волн G/I.
  - Валидатор графа `tools/verify-wave-graph.py:14` принимает ID только `[A-N][1-9]\d*`; B3, B4 и буква U свободны и в локальном графе, и в каноническом `../MoreFoto/docs/04-bitrix-modules/backend-waves.json`.
  - `EmailNotificationInputDto` принимает только plain-text `body`; `BitrixEmailTransport.php:26` экранирует HTML. Письмо кода регистрации (`Version20260326120009.php:12`) брендировано «RaBit API». `homePath` (`stores/auth.ts:180-184`) без `catalog.manage` ведёт на `/cabinet/profile`. Перехватчик 401 (`api/http.ts:49-57`) любой отзыв сессии показывает как «Сессия истекла».
- Ветка `codex/design-ux-plan` создана от актуального `main`; папка задачи `docs/plans/design-ux-plan/`.

### 2026-09-22 — аудит: лимит, перезапуски, сокращение агентов

- Workflow из 33 агентов (6 аудитов, 3 дизайн-направления → 3 судьи → синтез, 6 UX-тем × 2 проверки, планировщик волн, критик). Первый прогон завершил 4 аудита; остальные упали по лимиту сессии (сброс 21:10 МСК).
- 21:14 перезапуск с кешем; сессия Claude Code завершилась до окончания. 21:28 повторный перезапуск.
- По просьбе пользователя («можно ли сократить количество агентов») workflow остановлен в 21:57. К этому моменту завершились все 6 аудитов, 3 дизайн-направления, 3 судьи и синтез (14 результатов). UX-решения, разбиение на волны и критика полноты выполнены без агентов в основном потоке на основе аудитов.
- Итоги панели: «Галерея (editorial)» — 42/40/41 балла у трёх судей, «Тихая вода» — 37/41/40, «Море и свет» — 38/33/36. Синтез «Кадр и мазок v2» перенесён в раздел 7 плана вместе с пересчитанными контрастами.
- Ключевые выводы аудитов перенесены в раздел 6 плана; полные тексты остались в журнале workflow сессии (`subagents/workflows/wf_d604880e-4c7/journal.jsonl`), в репозиторий не добавлялись.

### 2026-09-22 — план и проверки

- Записан `plan.md`: 17 разделов — факты из кода, scope, аудит, токен-система (примитивы, семантика, радиусы, тени как выразительный инструмент, типографика, контрасты, соответствие Vuetify и миграция hex), логотип «Кадр и мазок», `MfAvatar` и backend аватарок, инфографика с картой INF-01…INF-12 и серверными сводками, UX доступа (приглашение по ссылке, сброс и смена пароля, коды ошибок, `reason=revoked`), профиль, каркас, система состояний, экраны, очистка наследия, волны U1–U8/B3/B4, решения DS-01…DS-19, риски, чек-лист, критерии приёмки, тест-кейсы DP-01…DP-05 и DX-* по волнам.
- Проверки DP-01…DP-05 выполнены (таблица выше). Исправлено: строка таблицы состояний с `|` внутри кода; уточнены команды DP-02/DP-03.
- Запущен один проверяющий агент: опровергнуть фактические утверждения плана по коду (пути и строки раздела 2, контракты разделов 8.3/10.1, свободные ID волн, DTO раздела 9.5, endpoint ID). Результат и исправления — ниже.

### 2026-09-22 — согласование с открытыми PR и публикация

- Найдены открытые PR с пересечениями: [#36](https://github.com/rebit-pro/rabit-api/pull/36) план дашбордов N1, [#48](https://github.com/rebit-pro/rabit-api/pull/48) план чата через MAX, [#47](https://github.com/rebit-pro/rabit-api/pull/47) массовая загрузка фото, [#35](https://github.com/rebit-pro/rabit-api/pull/35) ЮKassa и E6. План дополнен: U5/U6 объявлены ранним нефинансовым срезом, который PR #36 допускает при отдельном решении (DS-20); INF-06 без распределения по оплате до G1; чаты ссылаются на PR #48; U7 строится поверх PR #47.
- После правок DP-03 и DP-05 повторно PASS; план 565 строк.
- Проверяющий агент (сверка фактов плана с кодом) остановлен до отчёта по указанию пользователя из-за лимита сессии; результат отсутствует, повтор — при возобновлении.
- `gh api user` подтвердил доступ (`rebit-pro`). Commit `f0ce9b6`, push, PR [#50](https://github.com/rebit-pro/rabit-api/pull/50) создан с описанием плана, связей с PR #35/#36/#47/#48, проверок DP-01…DP-05 и ограничений.

### 2026-09-23 — решения и порядок доставки

- Пользователь взял план в работу. Первый вариант процесса («коммит + маленький E2E только на изменённое, общий gate после всех волн») заменён вторым: весь дизайн-пакет — одна ветка и один PR, волна — коммит, E2E и визуальная проверка — один раз после всех волн. Обоснование пользователя: меньше затрат на E2E, которое слишком усложняет разработку; сложность ревью принята.
- Решения пользователя: B4 — в общем PR отдельной серией коммитов; DS-01…DS-20 — все рекомендации плана (DS-10 термины по плану, DS-14 head читает списки, DS-15 одна сессия, DS-16 капча выключена, DS-20 ранний срез U5/U6 выполняется); PR #50 сливается агентом.
- По логам прогонов D3 (`api/var/e2e/*` в worktree D3) полный gate занимает 10–15 минут: `npm ci` 2–4 мин, браузер 5–8 мин; это основание оставить его один раз в конце.
- План обновлён: статус, раздел 5 (D01 закрывается разделом 7, не 6 — исправлена ссылка), раздел 12 (порядок доставки единым PR, быстрые проверки на коммите, пакет доставки в графе), раздел 13 (решения приняты), раздел 14 (риски единого PR), чек-лист, раздел 17 (где фиксируются результаты). DP-03 и DP-05 повторно — PASS.

