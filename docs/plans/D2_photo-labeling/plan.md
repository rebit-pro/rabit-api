# D2 — правки по code review

Статус: в работе. Ветка `codex/d2-photo-labeling`, PR [#21](https://github.com/rebit-pro/rabit-api/pull/21), base `c32b98e7c7407592c54c9ee382e994d7fa611955`.

## Цель

Закрыть замечания review D2 без расширения продуктового scope: добавить содержательные русские class-level phpDoc к UseCase и сервисам волны, зафиксировать техдолг остальных классов в GitHub issue и сделать обязательными сохраняемые plan/progress артефакты для любой ветки.

## В объёме

- `AssignPhotosUseCase`, `ListPhotosUseCase`, `SetGroupCoverUseCase`;
- `PhotoRowMapper` как Application Service волны;
- синхронные правила в корневых `AGENTS.md` и `CLAUDE.md`;
- два task-артефакта: этот `plan.md` и соседний `progress.md`;
- GitHub issue для остальных существующих UseCase и Application/Domain Service;
- локальные проверки изменённых PHP и документации;
- ответ на оба review-комментария после публикации исправлений.

## Вне объёма

- изменение поведения D2, API, схемы БД или frontend;
- массовое комментирование классов других волн — оно вынесено в issue #23;
- merge PR #21 и deployment.

## План

- [x] Прочитать оба комментария PR #21, локальные инструкции и эталон Orteka.
- [x] Создать issue #23 для остальных UseCase и сервисов.
- [x] Добавить в `AGENTS.md`/`CLAUDE.md` обязательный workflow plan/progress и правило русских phpDoc.
- [x] Создать сохраняемые `plan.md` и `progress.md` для D2.
- [x] Добавить class-level phpDoc к трём UseCase и `PhotoRowMapper`.
- [x] Обновить прогресс фактическими файлами и результатами проверок.
- [x] Выполнить lint, PHP CS Fixer dry-run и релевантные тесты.
- [ ] Проверить diff, закоммитить, отправить ветку и ответить в PR #21.

## Риски и ограничения

- Комментарии не должны пересказывать имя класса или обещать детали, которых нет в коде.
- Документационные правки не должны менять runtime-поведение D2.
- Полный E2E уже пройден для реализации; повторный объём выбирается по фактическому diff review-правок.
- Никаких production/stage действий.

## Приёмка и тест-кейсы

| ID | Проверка | Ожидаемый результат |
| --- | --- | --- |
| D2-R01 | Просмотреть четыре D2-класса | Перед каждым классом есть русский phpDoc: зачем класс нужен и что выполняет |
| D2-R02 | `cmp AGENTS.md CLAUDE.md` | Инструкции идентичны и не расходятся между агентами |
| D2-R03 | Проверить `docs/plans/D2_photo-labeling/` | В папке ровно обязательные `plan.md` и `progress.md`; тест-кейсы находятся в плане |
| D2-R04 | Проверить раздел «Точка продолжения» | Указаны ветка/PR/issue, последний шаг, следующий шаг, блокеры и команды |
| D2-R05 | PHP lint изменённых PHP-файлов | Все файлы проходят без синтаксических ошибок |
| D2-R06 | PHP CS Fixer dry-run по изменённым PHP-файлам | Нет файлов, требующих форматирования |
| D2-R07 | Релевантный PHPUnit/PHPStan прогон | Проверки проходят; функциональное поведение не изменено |
| D2-R08 | Проверить GitHub issue #23 | Отдельная задача содержит scope и приёмку остальных UseCase/Service |
| D2-R09 | `git diff --check` и состояние PR | Diff чистый, PR открыт, merge/deploy не выполнялись |
