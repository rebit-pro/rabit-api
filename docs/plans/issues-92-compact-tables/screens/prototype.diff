diff --git a/frontend/src/modules/morefoto/catalog/components/CatalogScreen.vue b/frontend/src/modules/morefoto/catalog/components/CatalogScreen.vue
index e52ff47..c211f10 100644
--- a/frontend/src/modules/morefoto/catalog/components/CatalogScreen.vue
+++ b/frontend/src/modules/morefoto/catalog/components/CatalogScreen.vue
@@ -8,7 +8,7 @@ import CatalogTable from './CatalogTable.vue';
 import { useCatalog } from '../useCatalog';
 import { useProductEditor } from '../useProductEditor';
 import type { CatalogProduct } from '../api';
-const { snapshot, page, pages, loading, error, reload } = useCatalog();
+const { snapshot, loading, error, reload } = useCatalog();
 const route = useRoute();
 const router = useRouter();
 // The tab lives in the address, so a reload or a shared link opens the same part of the catalog.
@@ -17,6 +17,9 @@ const tab = computed<'products' | 'conditions'>({
   set: (value) => void router.replace({ query: { ...route.query, tab: value === 'conditions' ? 'conditions' : undefined } })
 });
 const notice = shallowRef('');
+const selected = shallowRef<string[]>([]);
+const removeIds = shallowRef<string[]>([]);
+const removeNames = computed(() => (snapshot.value?.data.items ?? []).filter((p) => removeIds.value.includes(p.id)).map((p) => p.name));
 const {
   command,
   existingId,
@@ -61,14 +64,24 @@ function edit(product?: CatalogProduct): void {
     <v-alert v-if="notice" type="success" variant="tonal" role="status" class="mb-5">{{ notice }}</v-alert>
   </template>
   <template v-if="tab === 'products' && snapshot && !error">
-    <p class="mf-muted mb-4">Всего позиций: {{ snapshot.meta.total }}</p>
-    <CatalogTable :products="snapshot.data.items" :disabled="loading" @edit="edit" />
-    <nav v-if="pages > 1" class="mf-actions mt-5" aria-label="Страницы каталога">
-      <v-btn variant="outlined" :disabled="loading || page === 1" @click="reload(page - 1)">Предыдущая</v-btn>
-      <span role="status">Страница {{ page }} из {{ pages }}</span>
-      <v-btn variant="outlined" :disabled="loading || page === pages" @click="reload(page + 1)">Следующая</v-btn>
-    </nav>
+    <CatalogTable v-model:selected="selected" :products="snapshot.data.items" :disabled="loading" @edit="edit" @remove="removeIds = $event" />
   </template>
+  <v-dialog :model-value="removeIds.length > 0" max-width="520" @update:model-value="!$event && (removeIds = [])">
+    <v-card class="morefoto-app mf-panel catalog-remove" data-testid="catalog-remove-dialog">
+      <h2>{{ removeIds.length === 1 ? 'Удалить продукцию?' : 'Удалить продукцию: ' + removeIds.length + '?' }}</h2>
+      <ul>
+        <li v-for="name in removeNames" :key="name">{{ name }}</li>
+      </ul>
+      <p>
+        Позиция исчезнет из каталога и условий групп. Если её уже покупали, удалить нельзя — сервер ответит отказом, а позицию можно снять
+        с продажи: оформленные заказы сохранят её название и цену.
+      </p>
+      <div class="mf-actions">
+        <v-btn color="error">Удалить</v-btn>
+        <v-btn variant="outlined" @click="removeIds = []">Отмена</v-btn>
+      </div>
+    </v-card>
+  </v-dialog>
   <AdminDialog
     :open="!!command"
     :title="existingId ? 'Редактирование продукции' : 'Новая продукция'"
@@ -84,3 +97,13 @@ function edit(product?: CatalogProduct): void {
     <ProductFields v-if="command" v-model="command" :errors="errors" :existing="!!existingId" live />
   </AdminDialog>
 </template>
+<style scoped>
+.catalog-remove {
+  display: grid;
+  gap: 16px;
+  padding: 24px;
+}
+.catalog-remove ul {
+  margin-left: var(--mf-space-5);
+}
+</style>
diff --git a/frontend/src/modules/morefoto/catalog/components/CatalogTable.vue b/frontend/src/modules/morefoto/catalog/components/CatalogTable.vue
index e22506f..ea152bb 100644
--- a/frontend/src/modules/morefoto/catalog/components/CatalogTable.vue
+++ b/frontend/src/modules/morefoto/catalog/components/CatalogTable.vue
@@ -1,72 +1,217 @@
 <script setup lang="ts">
-import MfStatus from '@/components/status/MfStatus.vue';
-import { money } from '../../commerce/money';
+import { computed, shallowRef, watch } from 'vue';
+import UiDataTable from '../../ui/components/UiDataTable.vue';
+import { clampTablePage, sortTableRows } from '../../ui/table-values';
+import type { UiTableColumn, UiTableRow, UiTableSort } from '../../ui/table-types';
 import type { CatalogProduct } from '../api';
-withDefaults(defineProps<{ products: CatalogProduct[]; disabled: boolean; editable?: boolean }>(), { editable: true });
-defineEmits<{ edit: [product: CatalogProduct] }>();
+import type { ProductKind } from '../../commerce/types';
+const props = defineProps<{ products: CatalogProduct[]; disabled: boolean }>();
+const emit = defineEmits<{ edit: [product: CatalogProduct]; remove: [ids: string[]]; activate: [ids: string[], active: boolean] }>();
+const kindLabels: Record<ProductKind, string> = { physical: 'Печать', digital: 'Электронный', bundle: 'Комплект' };
+const kindIcons: Record<ProductKind, string> = {
+  physical: 'mdi-printer-outline',
+  digital: 'mdi-cloud-download-outline',
+  bundle: 'mdi-package-variant-closed'
+};
+const columns: UiTableColumn[] = [
+  { key: 'name', label: 'Продукция', sortable: true, primary: true, width: '34%' },
+  { key: 'kind', label: 'Тип', sortable: true, mobile: true, width: '11%' },
+  { key: 'price', label: 'Цена', type: 'money', sortable: true, mobile: true, width: '11%' },
+  { key: 'discount', label: 'Сотрудникам', sortable: true, width: '13%' },
+  {
+    key: 'status',
+    label: 'Статус',
+    type: 'status',
+    sortable: true,
+    mobile: true,
+    width: '13%',
+    statuses: { active: { label: 'В продаже', tone: 'success' }, off: { label: 'Отключено', tone: 'neutral' } }
+  }
+];
+const query = shallowRef('');
+const kind = shallowRef<ProductKind | null>(null);
+const status = shallowRef<'active' | 'off' | null>(null);
+const page = shallowRef(1);
+const pageSize = shallowRef(10);
+const sort = shallowRef<UiTableSort>({ key: 'name', direction: 'asc' });
+const selected = defineModel<string[]>('selected', { required: true });
+const byId = computed(() => new Map(props.products.map((product) => [product.id, product])));
+const rows = computed<UiTableRow[]>(() =>
+  props.products
+    .filter(
+      (product) =>
+        (!kind.value || product.kind === kind.value) &&
+        (!status.value || (product.active ? 'active' : 'off') === status.value) &&
+        product.name.toLocaleLowerCase('ru').includes(query.value.trim().toLocaleLowerCase('ru'))
+    )
+    .map((product) => ({
+      id: product.id,
+      name: product.name,
+      kind: kindLabels[product.kind],
+      price: product.price,
+      discount: product.staffDiscount ? '−50%' : '—',
+      status: product.active ? 'active' : 'off'
+    }))
+);
+const sorted = computed(() => sortTableRows(rows.value, columns, sort.value));
+const pageRows = computed(() => sorted.value.slice((page.value - 1) * pageSize.value, page.value * pageSize.value));
+watch([query, kind, status, pageSize, sort], () => {
+  page.value = 1;
+});
+watch(
+  () => rows.value.length,
+  (total) => {
+    page.value = clampTablePage(page.value, total, pageSize.value);
+  }
+);
+const kindItems = [{ title: 'Все типы', value: null }, ...Object.entries(kindLabels).map(([value, title]) => ({ value, title }))];
+const statusItems = [
+  { title: 'Все статусы', value: null },
+  { title: 'В продаже', value: 'active' },
+  { title: 'Отключено', value: 'off' }
+];
+function product(id: string): CatalogProduct {
+  return byId.value.get(id)!;
+}
 </script>
 <template>
-  <p v-if="!products.length" role="status" class="mf-panel">Ассортимент пуст. Добавьте первую продукцию.</p>
-  <div v-else class="catalog-grid" aria-label="Ассортимент">
-    <article v-for="product in products" :key="product.id" class="mf-panel catalog-product">
-      <div class="catalog-copy">
-        <h2>{{ product.name }}</h2>
-        <p class="mf-muted">{{ product.format }} · {{ product.unit }}</p>
-        <p v-if="product.description">{{ product.description }}</p>
+  <form class="catalog-filters mb-4" aria-label="Фильтры каталога" @submit.prevent>
+    <v-text-field
+      v-model="query"
+      label="Название"
+      prepend-inner-icon="mdi-magnify"
+      clearable
+      hide-details
+      density="compact"
+      @update:model-value="query = $event ?? ''"
+    />
+    <v-select v-model="kind" :items="kindItems" label="Тип" aria-label="Тип" hide-details density="compact" />
+    <v-select v-model="status" :items="statusItems" label="Статус" aria-label="Статус" hide-details density="compact" />
+  </form>
+  <UiDataTable
+    title="Продукция"
+    label-key="name"
+    density="compact"
+    actions-width="168px"
+    :columns="columns"
+    :rows="pageRows"
+    :total="rows.length"
+    :page="page"
+    :page-size="pageSize"
+    :sort="sort"
+    :selected="selected"
+    :empty-title="query || kind || status ? 'Ничего не найдено' : 'Ассортимент пуст'"
+    :empty-description="query || kind || status ? 'Измените или очистите фильтры.' : 'Добавьте первую продукцию.'"
+    @sort="sort = $event"
+    @page="page = $event"
+    @page-size="pageSize = $event"
+    @select="selected = $event"
+  >
+    <template #selection>
+      <div class="catalog-bulk">
+        <p>Выбрано: {{ selected.length }}</p>
+        <v-btn variant="outlined" density="compact" prepend-icon="mdi-eye-off-outline" :disabled="disabled" @click="emit('activate', selected, false)"
+          >Снять с продажи</v-btn
+        >
+        <v-btn variant="outlined" density="compact" prepend-icon="mdi-eye-outline" :disabled="disabled" @click="emit('activate', selected, true)"
+          >Вернуть в продажу</v-btn
+        >
+        <v-btn color="error" variant="outlined" density="compact" prepend-icon="mdi-delete-outline" :disabled="disabled" @click="emit('remove', selected)"
+          >Удалить</v-btn
+        >
+        <v-btn variant="text" density="compact" @click="selected = []">Снять выбор</v-btn>
       </div>
-      <div class="catalog-price">
-        <strong>{{ money(product.price) }}</strong>
-        <MfStatus :tone="product.active ? 'success' : 'neutral'">{{ product.active ? 'В продаже' : 'Отключено' }}</MfStatus>
-        <span v-if="product.staffDiscount" class="mf-muted">Скидка сотрудникам 50%</span>
+    </template>
+    <template #cell-name="{ row }">
+      <span class="catalog-name">
+        <span class="catalog-icon" :class="'catalog-icon--' + product(row.id).kind" aria-hidden="true"
+          ><v-icon :icon="kindIcons[product(row.id).kind]" size="20"
+        /></span>
+        <span class="catalog-name-text">
+          <strong>{{ row.name }}</strong>
+          <small :title="product(row.id).description"
+            >{{ [product(row.id).format, product(row.id).unit].filter(Boolean).join(' · ') }}<template v-if="product(row.id).description">
+              — {{ product(row.id).description }}</template
+            ></small
+          >
+        </span>
+      </span>
+    </template>
+    <template #actions="{ row }">
+      <div class="catalog-actions">
+        <v-btn variant="text" density="compact" :disabled="disabled" :aria-label="'Изменить ' + row.name" @click="emit('edit', product(row.id))"
+          >Изменить</v-btn
+        >
+        <v-btn
+          icon="mdi-delete-outline"
+          variant="text"
+          density="compact"
+          color="error"
+          :disabled="disabled"
+          :aria-label="'Удалить ' + row.name"
+          @click="emit('remove', [row.id])"
+        />
       </div>
-      <v-btn
-        v-if="editable !== false"
-        variant="outlined"
-        :disabled="disabled"
-        :aria-label="'Редактировать ' + product.name"
-        @click="$emit('edit', product)"
-        >Редактировать</v-btn
-      >
-    </article>
-  </div>
+    </template>
+  </UiDataTable>
 </template>
 <style scoped>
-.catalog-grid {
+.catalog-filters {
   display: grid;
-  gap: var(--mf-space-4);
+  grid-template-columns: minmax(220px, 2fr) minmax(150px, 1fr) minmax(150px, 1fr);
+  gap: 12px;
 }
-.catalog-product {
+.catalog-name {
   display: flex;
   align-items: center;
-  gap: var(--mf-space-6);
-}
-.catalog-copy {
-  flex: 1;
+  gap: var(--mf-space-3);
   min-width: 0;
-  overflow-wrap: anywhere;
 }
-.catalog-copy h2 {
-  font-size: var(--mf-text-lg);
+.catalog-icon {
+  display: inline-grid;
+  place-items: center;
+  flex: 0 0 36px;
+  width: 36px;
+  height: 36px;
+  border-radius: var(--mf-radius-sm);
+  background: var(--mf-color-primary-soft);
+  color: var(--mf-color-primary);
 }
-.catalog-copy p {
-  margin-top: var(--mf-space-2);
-  white-space: pre-wrap;
+.catalog-icon--digital {
+  background: var(--mf-tone-info-bg);
 }
-.catalog-price {
+.catalog-icon--bundle {
+  background: var(--mf-tone-success-bg);
+}
+.catalog-name-text {
   display: grid;
-  justify-items: start;
-  gap: var(--mf-space-2);
-  flex-shrink: 0;
+  min-width: 0;
+}
+.catalog-name-text strong {
+  font-weight: 600;
+}
+.catalog-name-text small {
+  overflow: hidden;
+  color: var(--mf-color-text-secondary);
   font-size: var(--mf-text-sm);
+  font-weight: 400;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+.catalog-actions {
+  display: flex;
+  align-items: center;
+  gap: var(--mf-space-1);
 }
-.catalog-price strong {
-  font-size: var(--mf-text-lg);
+.catalog-bulk {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: var(--mf-space-2);
 }
-@media (max-width: 700px) {
-  .catalog-product {
-    align-items: stretch;
-    flex-direction: column;
-    gap: var(--mf-space-4);
+@media (max-width: 760px) {
+  .catalog-filters {
+    grid-template-columns: 1fr;
   }
 }
 </style>
diff --git a/frontend/src/modules/morefoto/handoff/components/LinksScreen.vue b/frontend/src/modules/morefoto/handoff/components/LinksScreen.vue
index 2d685ff..085eb63 100644
--- a/frontend/src/modules/morefoto/handoff/components/LinksScreen.vue
+++ b/frontend/src/modules/morefoto/handoff/components/LinksScreen.vue
@@ -17,7 +17,11 @@ import '../handoff.css';
 import MfStatus from '@/components/status/MfStatus.vue';
 import MfTimeline from '@/components/viz/MfTimeline.vue';
 import { linkStatus } from '../../ui/statusTone';
-import GalleryPath from './GalleryPath.vue';
+import UiDataTable from '../../ui/components/UiDataTable.vue';
+import MfStatTile from '@/components/viz/MfStatTile.vue';
+import { plural } from '@/components/viz/measures';
+import { sortTableRows } from '../../ui/table-values';
+import type { UiTableColumn, UiTableRow, UiTableSort } from '../../ui/table-types';
 import { galleryPath, type GalleryStepKey } from '../galleryPath';
 const live = !isMockApiEnabled,
   authStore = useAuthStore(),
@@ -85,6 +89,61 @@ function open(group: LinkGroup, action: LinkCommand['action']) {
 function change(value: Partial<LinkCommand>) {
   if (command.value?.kind === 'link') Object.assign(command.value, value);
 }
+const columns: UiTableColumn[] = [
+  { key: 'name', label: 'Группа', sortable: true, primary: true, width: '28%' },
+  { key: 'status', label: 'Состояние', sortable: true, mobile: true, width: '19%' },
+  { key: 'sentAt', label: 'Передана', type: 'date', sortable: true, mobile: true, width: '11%' },
+  { key: 'closesAt', label: 'Приём до', type: 'date', sortable: true, mobile: true, width: '14%' },
+  { key: 'deliveryAt', label: 'Доставка', type: 'date', sortable: true, width: '11%' }
+];
+const statusFilter = shallowRef<string | null>(null);
+const institutionFilter = shallowRef<string | null>(null);
+const query = shallowRef('');
+const sort = shallowRef<UiTableSort>({ key: 'closesAt', direction: 'asc' });
+const selected = shallowRef<string[]>([]);
+const byId = computed(() => new Map((data.value?.groups ?? []).map((g) => [g.id, g])));
+const statusCounts = computed(() => {
+  const counts: Record<string, number> = { 'Требует проверки': 0, 'Готова к передаче': 0, 'Приём открыт': 0, 'Приём завершён': 0 };
+  for (const g of data.value?.groups ?? []) counts[linkStatus(g).text]++;
+  return counts;
+});
+const tiles = [
+  { text: 'Требует проверки', icon: 'mdi-alert-circle-outline', pastel: 4 },
+  { text: 'Готова к передаче', icon: 'mdi-send-check-outline', pastel: 3 },
+  { text: 'Приём открыт', icon: 'mdi-cart-outline', pastel: 2 },
+  { text: 'Приём завершён', icon: 'mdi-check-all', pastel: 7 }
+];
+const tableRows = computed<UiTableRow[]>(() =>
+  groups.value
+    .filter(
+      (g) =>
+        (!statusFilter.value || linkStatus(g).text === statusFilter.value) &&
+        (!institutionFilter.value || g.institutionId === institutionFilter.value) &&
+        (g.name + ' ' + g.shootName).toLocaleLowerCase('ru').includes(query.value.trim().toLocaleLowerCase('ru'))
+    )
+    .map((g) => ({ id: g.id, name: g.name, status: linkStatus(g).text, sentAt: g.sentAt, closesAt: g.closesAt, deliveryAt: g.deliveryAt }))
+);
+const sortedRows = computed(() => sortTableRows(tableRows.value, columns, sort.value));
+function institutionName(g: LinkGroup): string {
+  return data.value?.scope.institutions.find((i) => i.id === g.institutionId)?.name ?? '';
+}
+function daysLeft(g: LinkGroup): string {
+  if (!g.closesAt || !data.value || g.state === 'closed') return '';
+  const days = Math.ceil((Date.parse(g.closesAt) - Date.parse(data.value.now)) / 86400000);
+  return days > 0 ? 'осталось ' + days + ' ' + plural(days, ['день', 'дня', 'дней']) : 'закрывается сегодня';
+}
+function nextStep(g: LinkGroup): string {
+  const step = galleryPath(g).find((item) => item.state === 'current');
+  return step ? step.title : '';
+}
+function stepsDone(g: LinkGroup): string {
+  const steps = galleryPath(g);
+  return steps.filter((item) => item.state === 'done').length + ' из ' + steps.length;
+}
+const shortDate = new Intl.DateTimeFormat('ru-RU', { day: 'numeric', month: 'short', timeZone: 'Europe/Moscow' });
+function day(value: string | null): string {
+  return value ? shortDate.format(new Date(value)) : '—';
+}
 const url = (token: string) => window.location.origin + '/g/' + token;
 /** Live keys are requested per group on demand: the list never carries gallery tokens. */
 async function token(group: LinkGroup): Promise<string> {
@@ -144,82 +203,110 @@ async function history(group: LinkGroup, event: Event) {
   <v-alert v-if="failure" type="error" variant="tonal" role="alert" class="mb-5">{{ failure }}</v-alert>
   <v-alert v-if="error" type="error" variant="tonal">{{ error }}<v-btn variant="text" @click="reload">Повторить</v-btn></v-alert>
   <template v-if="data">
-    <v-select
-      v-model="filter"
-      label="Группа"
-      aria-label="Группа"
-      :items="[{ title: 'Все группы', value: '' }, ...data.groups.map((g) => ({ title: g.name + ' · ' + g.shootName, value: g.id }))]"
-      class="handoff-filter"
-    />
-    <p v-if="!groups.length" class="handoff-empty">Нет групп для выбранного фильтра.</p>
-    <article v-for="group in groups" :key="group.id" class="handoff-card" :data-testid="'link-' + group.id">
-      <header>
-        <div>
-          <p class="mf-muted">{{ data.scope.institutions.find((i) => i.id === group.institutionId)?.name }} · {{ group.shootName }}</p>
-          <h2>{{ group.name }}</h2>
-        </div>
-        <MfStatus :tone="linkStatus(group).tone">{{ linkStatus(group).text }}</MfStatus>
-      </header>
-      <MfTimeline
-        class="handoff-timeline"
-        :sent-at="group.sentAt"
-        :closes-at="group.closesAt"
-        :delivery-at="group.deliveryAt"
-        :now="data.now"
+    <div class="links-tiles mb-5">
+      <MfStatTile
+        v-for="tile in tiles"
+        :key="tile.text"
+        :label="tile.text"
+        :value="statusCounts[tile.text]"
+        :unit="plural(statusCounts[tile.text], ['группа', 'группы', 'групп'])"
+        :icon="tile.icon"
+        :pastel="tile.pastel"
+        selectable
+        :active="statusFilter === tile.text"
+        @select="statusFilter = statusFilter === tile.text ? null : tile.text"
+      />
+    </div>
+    <form class="links-filters mb-4" aria-label="Фильтры ссылок" @submit.prevent>
+      <v-text-field v-model="query" label="Группа или съёмка" prepend-inner-icon="mdi-magnify" clearable hide-details density="compact" />
+      <v-select
+        v-model="institutionFilter"
+        :items="[{ title: 'Все учреждения', value: null }, ...data.scope.institutions.map((i) => ({ title: i.name, value: i.id }))]"
+        label="Учреждение"
+        aria-label="Учреждение"
+        hide-details
+        density="compact"
+      />
+      <v-select
+        v-model="statusFilter"
+        :items="[{ title: 'Все состояния', value: null }, ...tiles.map((t) => ({ title: t.text, value: t.text }))]"
+        label="Состояние"
+        aria-label="Состояние"
+        hide-details
+        density="compact"
       />
-      <label v-if="!live" class="handoff-url"
-        >Ссылка группы<input
-          :value="url(group.galleryToken)"
-          readonly
-          :aria-label="'Ссылка группы ' + group.name"
-          @focus="($event.target as HTMLInputElement).select()"
-      /></label>
-      <GalleryPath v-if="live && !group.sentAt" :steps="galleryPath(group)" :fix="(key) => stepFix(group, key)" />
-      <p v-if="!group.sentAt" class="mf-muted mb-2">
-        {{
-          live && !group.prepared
-            ? 'Ссылка появится после проверки группы.'
-            : 'До отметки передачи родители видят «Фотографии ещё готовятся». Копирование ссылки не запускает срок.'
-        }}
-      </p>
-      <ul v-if="!live && !group.sentAt && group.problems.length" class="handoff-problems" aria-label="Что мешает открыть галерею">
-        <li v-for="problem in group.problems" :key="problem">
-          <v-icon icon="mdi-alert-circle-outline" size="18" aria-hidden="true" />
-          <span>{{ problemText(problem) }}</span>
-        </li>
-      </ul>
-      <div class="mf-actions">
-        <template v-if="!live || group.prepared">
-          <v-btn variant="outlined" @click="copy(group)">Копировать ссылку</v-btn
-          ><v-btn variant="text" @click="openGallery(group)">{{
-            group.sentAt ? 'Открыть галерею' : 'Посмотреть страницу родителей'
-          }}</v-btn>
-        </template>
-        <v-btn v-if="data.role === 'organizer' && !group.sentAt" :disabled="!!group.problems.length" @click="open(group, 'prepare')"
-          >Проверить ссылку</v-btn
-        >
-        <v-btn v-if="data.role !== 'head' && !group.sentAt && group.prepared" @click="open(group, 'transmit')">Отметить передачу</v-btn>
-        <v-btn v-if="['curator', 'organizer'].includes(data.role) && group.sentAt" variant="text" @click="open(group, 'correct')"
-          >Исправить дату</v-btn
-        >
-      </div>
-      <details v-if="live || group.history.length" class="handoff-history" @toggle="history(group, $event)">
-        <summary>История изменений{{ live ? '' : ' · ' + group.history.length }}</summary>
-        <ol>
-          <li v-for="(event, index) in histories[group.id] ?? group.history" :key="index">
-            <strong>{{
-              event.kind === 'prepared' ? 'Ссылка проверена' : event.kind === 'transmitted' ? 'Передача отмечена' : 'Дата исправлена'
-            }}</strong>
-            · {{ event.actorName ?? 'Сотрудник №' + event.actorId }} · {{ formatMoment(event.at) }}
-            <p v-if="event.sentAt">Передача: {{ formatMoment(event.sentAt) }} · приём до {{ formatMoment(event.closesAt) }}</p>
-            <p v-if="event.previousSentAt">
-              Прежняя передача: {{ formatMoment(event.previousSentAt) }} · приём до {{ formatMoment(event.previousClosesAt) }}
-            </p>
-            <p v-if="event.reason">{{ event.reason }}</p>
-          </li>
-        </ol>
-      </details>
-    </article>
+    </form>
+    <UiDataTable
+      title="Ссылки групп"
+      label-key="name"
+      density="compact"
+      actions-width="176px"
+      :columns="columns"
+      :rows="sortedRows"
+      :total="sortedRows.length"
+      :page="1"
+      :page-size="25"
+      :sort="sort"
+      :selected="selected"
+      empty-title="Нет групп"
+      empty-description="Измените фильтры."
+      @sort="sort = $event"
+      @select="selected = $event"
+    >
+      <template #selection>
+        <div class="links-bulk">
+          <p>Выбрано: {{ selected.length }}</p>
+          <v-btn variant="outlined" density="compact" prepend-icon="mdi-content-copy">Скопировать ссылки</v-btn>
+          <v-btn variant="text" density="compact" @click="selected = []">Снять выбор</v-btn>
+        </div>
+      </template>
+      <template #cell-name="{ row }">
+        <span class="links-group">
+          <strong>{{ row.name }}</strong>
+          <small>{{ institutionName(byId.get(row.id)!) }} · {{ byId.get(row.id)!.shootName }}</small>
+        </span>
+      </template>
+      <template #cell-status="{ row }">
+        <span class="links-state">
+          <MfStatus :tone="linkStatus(byId.get(row.id)!).tone">{{ row.status }}</MfStatus>
+          <small v-if="!byId.get(row.id)!.sentAt && nextStep(byId.get(row.id)!)"
+            >шаг {{ stepsDone(byId.get(row.id)!) }}: {{ nextStep(byId.get(row.id)!).toLowerCase() }}</small
+          >
+        </span>
+      </template>
+      <template #cell-sentAt="{ row }">{{ day(byId.get(row.id)!.sentAt) }}</template>
+      <template #cell-closesAt="{ row }">
+        <span class="links-state">
+          {{ day(byId.get(row.id)!.closesAt) }}
+          <small v-if="daysLeft(byId.get(row.id)!)" class="links-left">{{ daysLeft(byId.get(row.id)!) }}</small>
+        </span>
+      </template>
+      <template #cell-deliveryAt="{ row }">{{ day(byId.get(row.id)!.deliveryAt) }}</template>
+      <template #actions="{ row }">
+        <div class="links-actions">
+          <template v-if="byId.get(row.id)!.sentAt">
+            <v-btn icon="mdi-content-copy" variant="text" density="compact" :aria-label="'Копировать ссылку ' + row.name" v-tooltip="'Копировать ссылку'" @click="copy(byId.get(row.id)!)" />
+            <v-btn icon="mdi-open-in-new" variant="text" density="compact" :aria-label="'Открыть галерею ' + row.name" v-tooltip="'Открыть галерею'" @click="openGallery(byId.get(row.id)!)" />
+          </template>
+          <v-btn v-if="!byId.get(row.id)!.sentAt && !byId.get(row.id)!.prepared" variant="tonal" density="compact" color="primary">Проверить</v-btn>
+          <v-btn v-else-if="!byId.get(row.id)!.sentAt" variant="tonal" density="compact" color="primary">Передать</v-btn>
+          <v-menu>
+            <template #activator="{ props: menuProps }">
+              <v-btn v-bind="menuProps" icon="mdi-dots-vertical" variant="text" density="compact" :aria-label="'Действия ' + row.name" />
+            </template>
+            <v-list class="morefoto-app mf-ui-overlay" density="compact">
+              <template v-if="!byId.get(row.id)!.sentAt && byId.get(row.id)!.prepared">
+                <v-list-item prepend-icon="mdi-content-copy" title="Копировать ссылку" />
+                <v-list-item prepend-icon="mdi-open-in-new" title="Страница родителей" />
+              </template>
+              <v-list-item v-if="byId.get(row.id)!.sentAt" prepend-icon="mdi-calendar-edit-outline" title="Исправить дату передачи" />
+              <v-list-item prepend-icon="mdi-history" title="История изменений" />
+              <v-list-item prepend-icon="mdi-image-multiple-outline" title="К фотографиям" />
+            </v-list>
+          </v-menu>
+        </div>
+      </template>
+    </UiDataTable>
   </template>
   <AdminDialog
     :open="!!command"
@@ -244,3 +331,54 @@ async function history(group: LinkGroup, event: Event) {
       @change="change"
   /></AdminDialog>
 </template>
+<style scoped>
+.links-tiles {
+  display: grid;
+  grid-template-columns: repeat(auto-fit, minmax(min(100%, 150px), 1fr));
+  gap: var(--mf-space-4);
+}
+.links-filters {
+  display: grid;
+  grid-template-columns: minmax(220px, 2fr) minmax(180px, 1.4fr) minmax(170px, 1fr);
+  gap: 12px;
+}
+.links-group,
+.links-state {
+  display: grid;
+  justify-items: start;
+  gap: 2px;
+}
+.links-group strong {
+  font-weight: 600;
+}
+.links-group small {
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+  max-width: 100%;
+}
+.links-group small,
+.links-state small {
+  color: var(--mf-color-text-secondary);
+  font-size: var(--mf-text-sm);
+  font-weight: 400;
+}
+.links-state .links-left {
+  color: var(--mf-color-primary);
+  font-weight: 600;
+}
+.links-actions,
+.links-bulk {
+  display: flex;
+  align-items: center;
+  gap: 2px;
+}
+.links-bulk {
+  gap: var(--mf-space-3);
+}
+@media (max-width: 760px) {
+  .links-filters {
+    grid-template-columns: 1fr;
+  }
+}
+</style>
diff --git a/frontend/src/modules/morefoto/structure/components/InstitutionOverview.vue b/frontend/src/modules/morefoto/structure/components/InstitutionOverview.vue
index 14436f8..d07a3c6 100644
--- a/frontend/src/modules/morefoto/structure/components/InstitutionOverview.vue
+++ b/frontend/src/modules/morefoto/structure/components/InstitutionOverview.vue
@@ -1,81 +1,68 @@
 <script setup lang="ts">
 import { computed } from 'vue';
 import MfAvatar from '@/components/avatar/MfAvatar.vue';
-import MfDistribution from '@/components/viz/MfDistribution.vue';
-import { groupStateSegments } from '../../ui/groupStates';
+import MfStatTile from '@/components/viz/MfStatTile.vue';
+import { plural } from '@/components/viz/measures';
 import type { InstitutionDetail } from '../model';
 const props = defineProps<{ institution: InstitutionDetail }>();
-/** The responsible staff by their names (DS-11); an assignment without a name still says someone is assigned. */
 const people = computed(() => [
-  { key: 'curator', role: 'Куратор', id: props.institution.curatorId, name: props.institution.curatorName, none: 'Не назначен' },
-  { key: 'head', role: 'Руководитель учреждения', id: props.institution.headId, name: props.institution.headName, none: 'Не назначен' }
+  { key: 'curator', role: 'Куратор', id: props.institution.curatorId, name: props.institution.curatorName },
+  { key: 'head', role: 'Руководитель', id: props.institution.headId, name: props.institution.headName }
 ]);
-const states = computed(() => {
-  const counts = props.institution.groups.meta.summary?.byState;
-  return counts && props.institution.groups.meta.total ? groupStateSegments(counts) : null;
-});
+const byState = computed(() => props.institution.groups.meta.summary?.byState ?? { preparing: 0, open: 0, closed: 0 });
+const shoots = computed(() => props.institution.shoots.meta.total);
+const groups = computed(() => props.institution.groups.meta.total);
 </script>
 <template>
-  <section class="institution-overview" aria-label="Реквизиты и назначения" data-testid="institution-overview">
-    <h2>Об учреждении</h2>
-    <dl class="institution-facts">
+  <section class="institution-overview" aria-label="Сводка учреждения" data-testid="institution-overview">
+    <dl class="institution-facts mf-panel">
       <div class="institution-address">
-        <dt>Адрес</dt>
+        <dt><v-icon icon="mdi-map-marker-outline" size="18" aria-hidden="true" /> Адрес</dt>
         <dd>{{ institution.address || 'Адрес не указан' }}</dd>
       </div>
       <div v-for="person in people" :key="person.key" :data-testid="'institution-' + person.key">
         <dt>{{ person.role }}</dt>
-        <dd v-if="person.id === null" class="institution-none">{{ person.none }}</dd>
+        <dd v-if="person.id === null" class="institution-none">Не назначен</dd>
         <dd v-else class="institution-person">
           <MfAvatar :seed="String(person.id)" :name="person.name" :size="24" decorative />
           <span>{{ person.name ?? 'Имя не указано' }}</span>
         </dd>
       </div>
     </dl>
-    <MfDistribution
-      v-if="states"
-      class="institution-states"
-      title="Группы по состоянию приёма"
-      :segments="states"
-      :unit-forms="['группы', 'групп', 'групп']"
-    />
-  </section>
-  <section class="institution-summary" aria-label="Финансовая сводка" data-testid="institution-summary">
-    <h2>Финансовая сводка</h2>
-    <p v-if="institution.summary.availability === 'unavailable'" class="mf-muted" role="status">Финансовая сводка пока недоступна.</p>
+    <div class="institution-tiles">
+      <MfStatTile label="Съёмки" :value="shoots" :unit="plural(shoots, ['съёмка', 'съёмки', 'съёмок'])" icon="mdi-camera-outline" :pastel="3" />
+      <MfStatTile
+        label="Группы готовятся"
+        :value="byState.preparing"
+        :unit="'из ' + groups"
+        icon="mdi-progress-clock"
+        :pastel="5"
+      />
+      <MfStatTile label="Приём открыт" :value="byState.open" :unit="plural(byState.open, ['группа', 'группы', 'групп'])" icon="mdi-cart-outline" :pastel="2" />
+      <MfStatTile label="Финансы" icon="mdi-cash-multiple" unavailable />
+    </div>
   </section>
 </template>
 <style scoped>
-.institution-overview,
-.institution-summary {
-  padding: var(--mf-space-6);
-  border: 1px solid var(--mf-color-border);
-  border-radius: var(--mf-radius-md);
-  background: var(--mf-color-surface);
-}
-.institution-summary {
-  margin-top: var(--mf-space-4);
-}
-.institution-overview h2,
-.institution-summary h2 {
-  font-size: var(--mf-text-lg);
-}
-.institution-summary p {
-  margin-top: var(--mf-space-3);
+.institution-overview {
+  display: grid;
+  gap: var(--mf-space-4);
 }
 .institution-facts {
   display: grid;
   grid-template-columns: 2fr 1fr 1fr;
-  gap: var(--mf-space-6);
-  margin-top: var(--mf-space-5);
+  gap: var(--mf-space-5);
+  padding: var(--mf-space-4) var(--mf-space-5);
 }
 .institution-facts dt {
+  display: flex;
+  align-items: center;
+  gap: 4px;
   font-size: var(--mf-text-sm);
   color: var(--mf-color-text-secondary);
 }
 .institution-facts dd {
-  margin: var(--mf-space-2) 0 0;
-  white-space: pre-wrap;
+  margin: var(--mf-space-1) 0 0;
   overflow-wrap: anywhere;
 }
 .institution-person {
@@ -86,25 +73,17 @@ const states = computed(() => {
 .institution-none {
   color: var(--mf-color-text-secondary);
 }
-.institution-states {
-  margin-top: var(--mf-space-6);
+.institution-tiles {
+  display: grid;
+  grid-template-columns: repeat(auto-fit, minmax(min(100%, 150px), 1fr));
+  gap: var(--mf-space-4);
 }
 @media (max-width: 800px) {
   .institution-facts {
     grid-template-columns: 1fr 1fr;
-    gap: var(--mf-space-5);
   }
   .institution-address {
     grid-column: 1 / -1;
   }
 }
-@media (max-width: 600px) {
-  .institution-overview,
-  .institution-summary {
-    padding: var(--mf-space-5);
-  }
-  .institution-facts {
-    grid-template-columns: 1fr;
-  }
-}
 </style>
diff --git a/frontend/src/modules/morefoto/structure/components/InstitutionScreen.vue b/frontend/src/modules/morefoto/structure/components/InstitutionScreen.vue
index 7555e19..db9bf2d 100644
--- a/frontend/src/modules/morefoto/structure/components/InstitutionScreen.vue
+++ b/frontend/src/modules/morefoto/structure/components/InstitutionScreen.vue
@@ -4,7 +4,9 @@ import { useAuthStore } from '@/stores/auth';
 import MfBreadcrumbs from '@/components/navigation/MfBreadcrumbs.vue';
 import AdminDialog from '../../management/components/AdminDialog.vue';
 import InstitutionOverview from './InstitutionOverview.vue';
-import InstitutionCollection from './InstitutionCollection.vue';
+import StructureWidget from './StructureWidget.vue';
+import MfStatus from '@/components/status/MfStatus.vue';
+import type { UiTableColumn, UiTableRow } from '../../ui/table-types';
 import StructureFields from './StructureFields.vue';
 import { useInstitutionPage } from '../useInstitutionPage';
 import { useStructureEditor } from '../useStructureEditor';
@@ -59,6 +61,66 @@ async function editGroup(item?: StructureItem): Promise<void> {
   const group = item as Group | undefined;
   editor.open('group', group, group?.shootId ?? defaultShootId(shoots.value));
 }
+const shootColumns: UiTableColumn[] = [
+  { key: 'name', label: 'Съёмка', sortable: true, primary: true, width: '52%' },
+  { key: 'date', label: 'Дата', type: 'date', sortable: true, mobile: true, width: '18%' },
+  { key: 'groups', label: 'Групп', type: 'number', sortable: true, mobile: true, width: '10%' }
+];
+const groupColumns: UiTableColumn[] = [
+  { key: 'name', label: 'Группа', sortable: true, primary: true, width: '30%' },
+  { key: 'shoot', label: 'Съёмка', sortable: true, mobile: true, width: '25%' },
+  {
+    key: 'status',
+    label: 'Приём',
+    type: 'status',
+    sortable: true,
+    mobile: true,
+    width: '17%',
+    statuses: {
+      preparing: { label: 'Готовится', tone: 'info' },
+      open: { label: 'Приём открыт', tone: 'success' },
+      closed: { label: 'Приём завершён', tone: 'neutral' }
+    }
+  },
+  { key: 'closesAt', label: 'Приём до', type: 'date', sortable: true }
+];
+const shootFilter = shallowRef<string | null>(null);
+const removal = shallowRef<{ kind: 'shoot' | 'group'; names: string[] } | null>(null);
+function askRemove(kind: 'shoot' | 'group', ids: string[]): void {
+  const items = kind === 'shoot' ? (snapshot.value?.shoots.items ?? []) : (snapshot.value?.groups.items ?? []);
+  removal.value = { kind, names: items.filter((item) => ids.includes(item.id)).map((item) => item.name) };
+}
+const shootName = computed(() => new Map((snapshot.value?.shoots.items ?? []).map((shoot) => [shoot.id, shoot.name])));
+const shootRows = computed<UiTableRow[]>(() =>
+  (snapshot.value?.shoots.items ?? []).map((shoot) => ({
+    id: shoot.id,
+    name: shoot.name,
+    date: shoot.date,
+    groups: (snapshot.value?.groups.items ?? []).filter((group) => group.shootId === shoot.id).length
+  }))
+);
+const groupRows = computed<UiTableRow[]>(() =>
+  (snapshot.value?.groups.items ?? [])
+    .filter((group) => !shootFilter.value || group.shootId === shootFilter.value)
+    .map((group) => ({
+      id: group.id,
+      name: group.name,
+      shoot: shootName.value.get(group.shootId) ?? '',
+      kind: group.groupKind,
+      status: group.status,
+      closesAt: group.closesAt
+    }))
+);
+const shootFilterItems = computed(() => [
+  { title: 'Все съёмки', value: null },
+  ...(snapshot.value?.shoots.items ?? []).map((shoot) => ({ title: shoot.name, value: shoot.id }))
+]);
+function shootPath(id: string, tail = ''): string {
+  return '/cabinet/institutions/' + encodeURIComponent(props.institutionId) + '/shoots/' + encodeURIComponent(id) + tail;
+}
+function shootOf(groupId: string): string {
+  return snapshot.value?.groups.items.find((group) => group.id === groupId)?.shootId ?? '';
+}
 function editInstitution(): void {
   if (canManage.value && !disabled.value && snapshot.value) {
     notice.value = '';
@@ -73,8 +135,9 @@ function editInstitution(): void {
     <h1 class="institution-title">{{ snapshot?.name ?? 'Учреждение' }}</h1>
     <p class="mf-muted">Реквизиты, назначения, съёмки и группы учреждения</p>
     <div class="mf-actions mt-5">
-      <v-btn v-if="canManage" variant="outlined" :disabled="disabled" @click="editInstitution">Редактировать учреждение</v-btn>
-      <v-btn variant="outlined" :disabled="loading" @click="reload()">Обновить список</v-btn>
+      <v-btn v-if="canManage" variant="outlined" prepend-icon="mdi-pencil-outline" :disabled="disabled" @click="editInstitution">Изменить учреждение</v-btn>
+      <v-btn variant="outlined" prepend-icon="mdi-refresh" :disabled="loading" @click="reload()">Обновить</v-btn>
+      <v-btn v-if="canManage" variant="text" color="error" prepend-icon="mdi-delete-outline" :disabled="disabled">Удалить учреждение</v-btn>
     </div>
   </header>
   <v-progress-linear v-if="loading" indeterminate aria-label="Загрузка учреждения" class="mb-5" />
@@ -83,30 +146,87 @@ function editInstitution(): void {
   <v-alert v-if="notice && !error" type="success" variant="tonal" role="status" class="mb-5">{{ notice }}</v-alert>
   <template v-if="snapshot && !error">
     <InstitutionOverview :institution="snapshot" />
-    <InstitutionCollection
-      :institution-id="institutionId"
-      kind="shoot"
-      :items="snapshot.shoots.items"
-      :meta="snapshot.shoots.meta"
-      :disabled="disabled"
-      :can-manage="canManage"
-      @create="editShoot()"
-      @edit="editShoot"
-      @page="reload({ shootsPage: $event })"
-    />
-    <InstitutionCollection
-      :institution-id="institutionId"
-      kind="group"
-      :items="snapshot.groups.items"
-      :meta="snapshot.groups.meta"
-      :disabled="disabled || shootsLoading"
-      :can-manage="canManage"
-      :has-shoots="snapshot.shoots.meta.total > 0"
-      @create="editGroup()"
-      @edit="editGroup"
-      @page="reload({ groupsPage: $event })"
-    />
+    <div class="institution-widgets">
+      <StructureWidget
+        title="Съёмки"
+        icon="mdi-camera-outline"
+        create-label="Новая съёмка"
+        :columns="shootColumns"
+        :rows="shootRows"
+        :initial-sort="{ key: 'date', direction: 'desc' }"
+        actions-width="184px"
+        :disabled="disabled"
+        :can-manage="canManage"
+        empty-title="Съёмок пока нет"
+        empty-text="Добавьте съёмку — затем в ней появятся группы."
+        @create="editShoot()"
+        @remove="askRemove('shoot', $event)"
+      >
+        <template #cell-name="{ row }">
+          <RouterLink :to="shootPath(row.id)" class="institution-link">{{ row.name }}</RouterLink>
+        </template>
+        <template #actions="{ row }">
+          <div class="institution-row-actions">
+            <v-btn icon="mdi-image-multiple-outline" variant="text" density="compact" :to="shootPath(row.id, '/photos')" :aria-label="'Кадры: ' + row.name" v-tooltip="'Кадры'" />
+            <v-btn icon="mdi-tag-outline" variant="text" density="compact" :to="shootPath(row.id, '/conditions')" :aria-label="'Условия: ' + row.name" v-tooltip="'Условия продажи'" />
+            <v-btn icon="mdi-pencil-outline" variant="text" density="compact" :aria-label="'Изменить ' + row.name" v-tooltip="'Изменить'" @click="editShoot(snapshot.shoots.items.find((s) => s.id === row.id))" />
+            <v-btn icon="mdi-delete-outline" variant="text" density="compact" color="error" :aria-label="'Удалить ' + row.name" v-tooltip="'Удалить'" @click="askRemove('shoot', [row.id])" />
+          </div>
+        </template>
+      </StructureWidget>
+      <StructureWidget
+        title="Группы"
+        icon="mdi-account-group-outline"
+        create-label="Новая группа"
+        :columns="groupColumns"
+        :rows="groupRows"
+        :initial-sort="{ key: 'name', direction: 'asc' }"
+        actions-width="140px"
+        :disabled="disabled || shootsLoading || !snapshot.shoots.meta.total"
+        :can-manage="canManage"
+        empty-title="Групп пока нет"
+        empty-text="Добавьте группу: у каждой будут свои фотографии и ссылка для родителей."
+        @create="editGroup()"
+        @remove="askRemove('group', $event)"
+      >
+        <template #tools>
+          <v-select v-model="shootFilter" :items="shootFilterItems" label="Съёмка" aria-label="Съёмка" density="compact" hide-details class="institution-shoot-filter" />
+        </template>
+        <template #cell-name="{ row }">
+          <span class="institution-group">
+            <RouterLink :to="shootPath(shootOf(row.id), '/photos?group=' + row.id)" class="institution-link">{{ row.name }}</RouterLink>
+            <small v-if="row.kind === 'staff'">сотрудники</small>
+          </span>
+        </template>
+        <template #actions="{ row }">
+          <div class="institution-row-actions">
+            <v-btn icon="mdi-link-variant" variant="text" density="compact" :to="'/cabinet/links?group=' + row.id" :aria-label="'Ссылка: ' + row.name" v-tooltip="'Ссылка и сроки'" />
+            <v-btn icon="mdi-pencil-outline" variant="text" density="compact" :aria-label="'Изменить ' + row.name" v-tooltip="'Изменить'" @click="editGroup(snapshot.groups.items.find((g) => g.id === row.id))" />
+            <v-btn icon="mdi-delete-outline" variant="text" density="compact" color="error" :aria-label="'Удалить ' + row.name" v-tooltip="'Удалить'" @click="askRemove('group', [row.id])" />
+          </div>
+        </template>
+      </StructureWidget>
+    </div>
   </template>
+  <v-dialog :model-value="!!removal" max-width="560" @update:model-value="!$event && (removal = null)">
+    <v-card v-if="removal" class="morefoto-app mf-panel institution-remove" data-testid="structure-remove-dialog">
+      <h2>{{ removal.names.length === 1 ? (removal.kind === 'shoot' ? 'Удалить съёмку?' : 'Удалить группу?') : (removal.kind === 'shoot' ? 'Удалить съёмки: ' : 'Удалить группы: ') + removal.names.length + '?' }}</h2>
+      <ul>
+        <li v-for="name in removal.names" :key="name">{{ name }}</li>
+      </ul>
+      <p>
+        {{ removal.kind === 'shoot' ? 'Вместе со съёмкой удалятся её группы, ' : 'Вместе с группой удалятся ' }}загруженные кадры, коды детей, условия
+        продажи, ссылка для родителей и её история. Файлы кадров будут стёрты с диска. Отменить удаление нельзя.
+      </p>
+      <v-alert type="warning" variant="tonal" density="compact">
+        Если по группе уже есть заказы, сервер откажет: такие группы сохраняются для истории заказов и оплат.
+      </v-alert>
+      <div class="mf-actions">
+        <v-btn color="error">Удалить</v-btn>
+        <v-btn variant="outlined" @click="removal = null">Отмена</v-btn>
+      </div>
+    </v-card>
+  </v-dialog>
   <AdminDialog
     :open="!!draft"
     :title="title"
@@ -135,4 +255,37 @@ function editInstitution(): void {
 .institution-title {
   overflow-wrap: anywhere;
 }
+.institution-widgets {
+  display: grid;
+  gap: var(--mf-space-5);
+  margin-top: var(--mf-space-5);
+}
+.institution-row-actions {
+  display: flex;
+  align-items: center;
+  gap: 2px;
+}
+.institution-link {
+  color: inherit;
+  text-underline-offset: 3px;
+}
+.institution-group {
+  display: grid;
+}
+.institution-group small {
+  color: var(--mf-color-text-secondary);
+  font-size: var(--mf-text-sm);
+  font-weight: 400;
+}
+.institution-remove {
+  display: grid;
+  gap: 16px;
+  padding: 24px;
+}
+.institution-remove ul {
+  margin-left: var(--mf-space-5);
+}
+.institution-shoot-filter {
+  width: 260px;
+}
 </style>
diff --git a/frontend/src/modules/morefoto/structure/components/StructureWidget.vue b/frontend/src/modules/morefoto/structure/components/StructureWidget.vue
new file mode 100644
index 0000000..a3511b0
--- /dev/null
+++ b/frontend/src/modules/morefoto/structure/components/StructureWidget.vue
@@ -0,0 +1,129 @@
+<script setup lang="ts">
+import { computed, shallowRef, watch } from 'vue';
+import UiDataTable from '../../ui/components/UiDataTable.vue';
+import { clampTablePage, sortTableRows } from '../../ui/table-values';
+import type { UiTableColumn, UiTableRow, UiTableSort } from '../../ui/table-types';
+/** Presentation-only widget of the institution page: one compact table of shoots or groups with its own add button. */
+const props = defineProps<{
+  title: string;
+  icon: string;
+  createLabel: string;
+  columns: UiTableColumn[];
+  rows: UiTableRow[];
+  emptyTitle: string;
+  emptyText: string;
+  disabled: boolean;
+  canManage: boolean;
+  initialSort: UiTableSort;
+  actionsWidth: string;
+}>();
+const emit = defineEmits<{ create: []; remove: [ids: string[]] }>();
+const page = shallowRef(1);
+const pageSize = shallowRef(10);
+const sort = shallowRef<UiTableSort>(props.initialSort);
+const selected = shallowRef<string[]>([]);
+const sorted = computed(() => sortTableRows(props.rows, props.columns, sort.value));
+const pageRows = computed(() => sorted.value.slice((page.value - 1) * pageSize.value, page.value * pageSize.value));
+watch([pageSize, sort], () => {
+  page.value = 1;
+});
+watch(
+  () => props.rows.length,
+  (total) => {
+    page.value = clampTablePage(page.value, total, pageSize.value);
+  }
+);
+</script>
+<template>
+  <section class="mf-panel structure-widget" :aria-label="title">
+    <header class="structure-widget__head">
+      <h2><v-icon :icon="icon" size="22" aria-hidden="true" /> {{ title }} <span class="structure-widget__count">{{ rows.length }}</span></h2>
+      <div class="structure-widget__tools">
+        <slot name="tools" />
+        <v-btn v-if="canManage" prepend-icon="mdi-plus" density="comfortable" :disabled="disabled" @click="emit('create')">{{
+          createLabel
+        }}</v-btn>
+      </div>
+    </header>
+    <UiDataTable
+      :title="title"
+      label-key="name"
+      density="compact"
+      auto-pager
+      :actions-width="actionsWidth"
+      :columns="columns"
+      :rows="pageRows"
+      :total="rows.length"
+      :page="page"
+      :page-size="pageSize"
+      :sort="sort"
+      :selected="selected"
+      :selectable="canManage"
+      :empty-title="emptyTitle"
+      :empty-description="emptyText"
+      @sort="sort = $event"
+      @page="page = $event"
+      @page-size="pageSize = $event"
+      @select="selected = $event"
+    >
+      <template #selection>
+        <div class="structure-widget__bulk">
+          <p>Выбрано: {{ selected.length }}</p>
+          <v-btn
+            color="error"
+            variant="outlined"
+            density="compact"
+            prepend-icon="mdi-delete-outline"
+            :disabled="disabled"
+            @click="emit('remove', selected)"
+            >Удалить</v-btn
+          >
+          <v-btn variant="text" density="compact" @click="selected = []">Снять выбор</v-btn>
+        </div>
+      </template>
+      <template v-for="column in columns" #[`cell-${column.key}`]="{ row }" :key="column.key">
+        <slot :name="'cell-' + column.key" :row="row" />
+      </template>
+      <template #actions="{ row }"><slot name="actions" :row="row" /></template>
+    </UiDataTable>
+  </section>
+</template>
+<style scoped>
+.structure-widget {
+  min-width: 0;
+  padding: var(--mf-space-5);
+}
+.structure-widget__head {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--mf-space-3);
+  margin-bottom: var(--mf-space-2);
+}
+.structure-widget__head h2 {
+  display: flex;
+  align-items: center;
+  gap: var(--mf-space-2);
+  font-size: var(--mf-text-lg);
+}
+.structure-widget__count {
+  padding: 0 8px;
+  border-radius: 999px;
+  background: var(--mf-color-surface-2);
+  color: var(--mf-color-text-secondary);
+  font-size: var(--mf-text-sm);
+  font-weight: 600;
+}
+.structure-widget__tools {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: var(--mf-space-3);
+}
+.structure-widget__bulk {
+  display: flex;
+  align-items: center;
+  gap: var(--mf-space-3);
+}
+</style>
diff --git a/frontend/src/modules/morefoto/ui/components/UiDataTable.vue b/frontend/src/modules/morefoto/ui/components/UiDataTable.vue
index fd6a7e3..17909c4 100644
--- a/frontend/src/modules/morefoto/ui/components/UiDataTable.vue
+++ b/frontend/src/modules/morefoto/ui/components/UiDataTable.vue
@@ -26,8 +26,12 @@ const props = withDefaults(
     emptyDescription?: string;
     /** Column whose value names a row for assistive labels; the row id by default. */
     labelKey?: string;
+    /** Width of the actions column on desktop. */
+    actionsWidth?: string;
+    /** Hide the pager while everything fits on the smallest page (compact widgets). */
+    autoPager?: boolean;
   }>(),
-  { selectable: true, removable: true, density: 'comfortable', emptyTitle: 'Список пока пуст', emptyDescription: 'Здесь появятся записи.' }
+  { selectable: true, removable: true, density: 'comfortable', emptyTitle: 'Список пока пуст', emptyDescription: 'Здесь появятся записи.', actionsWidth: '176px', autoPager: false }
 );
 const emit = defineEmits<{
   sort: [sort: UiTableSort];
@@ -133,6 +137,11 @@ defineExpose({ focusRow });
               summary
             }}
           </caption>
+          <colgroup>
+            <col v-if="selectable" class="ui-table-check-col" />
+            <col v-for="column in columns" :key="column.key" :style="column.width ? { width: column.width } : undefined" />
+            <col :style="{ width: actionsWidth }" />
+          </colgroup>
           <thead>
             <tr>
               <th v-if="selectable" class="ui-table-check" scope="col">
@@ -240,7 +249,7 @@ defineExpose({ focusRow });
           /></slot>
         </article>
       </div>
-      <nav class="ui-table-pagination" :aria-label="'Страницы: ' + title">
+      <nav v-if="!autoPager || total > 10" class="ui-table-pagination" :aria-label="'Страницы: ' + title">
         <p data-testid="ui-table-range" role="status">{{ range }} из {{ total }}</p>
         <v-select
           :model-value="pageSize"
@@ -344,9 +353,8 @@ th {
   width: 52px;
   padding: 4px;
 }
-th:last-child,
-td:last-child {
-  width: 176px;
+.ui-table-check-col {
+  width: 52px;
 }
 .ui-table-primary {
   font-weight: 600;
diff --git a/frontend/src/modules/morefoto/ui/table-types.ts b/frontend/src/modules/morefoto/ui/table-types.ts
index 4c83231..7aebe40 100644
--- a/frontend/src/modules/morefoto/ui/table-types.ts
+++ b/frontend/src/modules/morefoto/ui/table-types.ts
@@ -12,6 +12,8 @@ export interface UiTableColumn {
   primary?: boolean;
   mobile?: boolean;
   statuses?: Record<string, { label: string; tone: UiTableTone }>;
+  /** Desktop column width, e.g. '30%' or '120px'; the rest share the remaining space. */
+  width?: string;
 }
 export interface UiTableSort {
   key: string;
diff --git a/frontend/src/plugins/icons.ts b/frontend/src/plugins/icons.ts
index d160059..ce190cf 100644
--- a/frontend/src/plugins/icons.ts
+++ b/frontend/src/plugins/icons.ts
@@ -1,6 +1,16 @@
 // Material Design Icons used by the cabinet: SVG paths from @mdi/js instead of the 400 KB icon font.
 // Add an icon here before using its `mdi-*` name; tests/ui/icons.test.mjs checks that nothing is missing.
 import {
+  mdiCloudDownloadOutline,
+  mdiPackageVariantClosed,
+  mdiPencilOutline,
+  mdiRefresh,
+  mdiProgressClock,
+  mdiOpenInNew,
+  mdiCheckAll,
+  mdiHistory,
+  mdiCalendarEditOutline,
+  mdiMapMarkerOutline,
   mdiAccount,
   mdiAccountCheckOutline,
   mdiAccountClockOutline,
@@ -79,6 +89,16 @@ import {
 } from '@mdi/js';
 
 export const MF_ICONS: Readonly<Record<string, string>> = {
+  'mdi-cloud-download-outline': mdiCloudDownloadOutline,
+  'mdi-package-variant-closed': mdiPackageVariantClosed,
+  'mdi-pencil-outline': mdiPencilOutline,
+  'mdi-refresh': mdiRefresh,
+  'mdi-progress-clock': mdiProgressClock,
+  'mdi-open-in-new': mdiOpenInNew,
+  'mdi-check-all': mdiCheckAll,
+  'mdi-history': mdiHistory,
+  'mdi-calendar-edit-outline': mdiCalendarEditOutline,
+  'mdi-map-marker-outline': mdiMapMarkerOutline,
   'mdi-account': mdiAccount,
   'mdi-account-check-outline': mdiAccountCheckOutline,
   'mdi-account-clock-outline': mdiAccountClockOutline,
